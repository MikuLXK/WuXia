import { 提示词结构 } from '../../types';

export const 核心_输出格式_多重思考: 提示词结构 = {
    id: 'core_format_multi',
    标题: '输出格式 - 多重思考',
    内容: `
<多重思考输出格式>
# 【多重思考版输出格式 | 全量对齐普通格式拆分版】

## 0) JSON 硬约束
- 仅输出 1 个 JSON 对象；禁止 JSON 外解释文本。
- 字段顺序固定，不得乱序：
  1. \`t_input\`
  2. \`t_plan\`
  3. \`t_state\`
  4. \`t_branch\`
  5. \`t_precheck\`
  6. \`logs\`
  7. \`t_logcheck\`
  8. \`t_var\`
  9. \`t_npc\`
  10. \`t_cmd\`
  11. \`t_audit\`
  12. \`t_fix\`
  13. \`tavern_commands\`
  14. \`t_mem\`
  15. \`shortTerm\`
  16. \`t_opts\`
  17. \`action_options\`（开关开启时）
- 每个 \`t_*\` 字段必须是字符串，且必须使用 \`<thinking>...</thinking>\` 包裹。
- \`shortTerm\` 必须是字符串；\`logs\` 与 \`tavern_commands\` 必须是数组。

## 1) t_* 字段最小信息密度（强制）
- \`t_input\`：玩家目标/边界/禁止项 + [用户指令]解析结论。
- \`t_plan\`：回合类型、推进目标、冲突强度、收束点。
- \`t_state\`：关键状态快照（时间地点、资源、剧情门槛、战斗态、风险）。
- \`t_branch\`：A/B/C 三分支 + 选用理由 + 放弃理由。
- \`t_precheck\`：候选命令预演（前值->后值 + 合法/非法 + 理由），并包含时间推进、剧情钩子、世界动态、NPC 在场预检。
- \`t_logcheck\`：正文一致性/越权/因果复核。
- \`t_var\`：正文事实 -> 变量变化映射清单。
- \`t_npc\`：在场集合、入离场、记忆/关系同步、重要女性完整性复核。
- \`t_cmd\`：最终命令执行顺序方案（创建->修改->清理，及特殊模块顺序）。
- \`t_audit\`：路径、类型、索引、格式、上限下限、叙事一致性总审计。
- \`t_fix\`：删除非法候选并给出修正后可执行方向。
- \`t_mem\`：短期记忆提炼策略（行动-结果-影响-未决点）。
- \`t_opts\`：选项筛选逻辑（可执行、非剧透、互补）。

## 2) logs 字段规范（对齐普通格式）
- \`logs\` 元素固定结构：
  - \`{"sender":"旁白|角色名|【判定】|【NSFW判定】","text":"..."}\`
- 正文只能使用一个 \`logs\` 字段（统一容器）；禁止拆出 \`narration_logs/dialogue_logs\` 等平行字段。
- 禁止 \`sender=系统\`。
- 旁白/对话分离：
  - \`sender=旁白\` 只写叙述，不得含直接引语（\`“...”\`/\`「...」\`/\`『...』\`）或“角色名+冒号”发言格式。
  - \`sender=角色名\` 只写台词，不得夹带动作与环境叙述。
  - **角色发言必须另起一行**：每次发言必须是单独一条 \`logs\`，不得与旁白共享同一条 \`text\`。
  - 这里“拆成两条 logs”仅指：在同一 \`logs\` 数组内追加两个对象元素，不是新增第二个 \`logs\` 字段。
  - 若出现“某人说道 + 引号内容”，必须拆成“旁白动作”与“角色台词”两条，禁止混写。
  - 违规示例（禁止）：\`{"sender":"旁白","text":"店小二赔笑道：'客官里边请。'"}\`
  - 合规示例（必须）：
    - \`{"sender":"旁白","text":"店小二赔着笑，侧身让出门道。"}\`
    - \`{"sender":"店小二","text":"客官里边请。"}\`
- 判定必须单独一条 logs，且格式固定：
  - \`行动名称｜结果｜触发对象 玩家:姓名 或 NPC:姓名｜判定值 X/难度 Y｜基础 B (说明)｜环境 E (说明)｜状态 S (说明)｜幸运 L\`
- logs 中未发生事实，不得在命令里提前落地。

## 3) tavern_commands 字段规范（对齐普通格式）
- 命令结构固定：
  - \`{"action":"add|set|push|delete","key":"gameState.xxx","value":...}\`
- 仅允许操作：\`add/set/push/delete\`。
- 可写域：\`gameState.角色/环境/社交/世界/战斗/剧情/女主剧情规划(开启时)\`
- 禁写：\`gameState.背包\`
- 命令顺序强制：创建 -> 修改 -> 清理。
- 任何非法候选命令不得进入最终数组。

## 4) 必须生成指令的场景（多重思考版）
- 时间推进/跨日：
  - \`set gameState.环境.时间\`（\`YYYY:MM:DD:HH:MM\`）
  - 跨日再 \`set gameState.环境.游戏天数\`
- 位置变化：
  - 同小地点微移动可仅改 \`具体地点\`
  - 跨层级移动必须联动 \`大地点/中地点/小地点/具体地点\`
- 物品获得（叙事明确拾取/装入）：
  - \`push gameState.角色.物品列表\`（完整对象）
  - 同步 \`当前容器ID\`；装备态同步 \`当前装备部位\`
  - 同步容器 \`当前已用空间\`（及必要的容器占用变化）
- 掉落未拾取：
  - 只写 logs，不写物品归属命令
- 物品消耗：
  - 单件建模：\`delete gameState.角色.物品列表[i]\`
  - 具备数量字段时：先 \`add 数量 -1\`，归零后再删
- 战斗/体力/受伤：
  - 部位血量与精力变动需落地 \`add\`
- 社交/NPC：
  - 好感、关系、在场、队伍状态变化需落地
  - 有效互动后写入 \`gameState.社交[i].记忆\`
  - 新建档必须先查重并满足建档条件
- 剧情推进：
  - \`剧情变量\`、\`待触发事件\`、\`历史卷宗\` 等变化需按事实落地

## 5) 前置依赖与合法性检查（强制）
- 父对象存在性：不存在先创建后修改。
- 索引合法：\`社交[i]\`、\`物品列表[i]\` 必须来自确认映射。
- 类型匹配：\`add\` 数值、\`push\` 数组、\`set\` 类型兼容。
- 下限/上限检查：扣减不低于下限，增加不超过上限字段。
- 容量检查：容器存在、单件大小合法、总容量不超限。
- 时间格式检查：所有时间字段必须标准格式。
- 叙事一致性：正文发生才可下命令，正文未发生不得落地。

## 6) NPC 与重要女性门禁（强制）
- 必须先构建“本回合在场角色集合”后再写 NPC 互动命令。
- 未在场 NPC 默认不得写本场景直接互动记忆/好感（远程互动需有明确依据）。
- 命中 \`性别=女 且 是否主要角色=true\` 时，命令必须覆盖缺项补齐：
  - 关系驱动：\`核心性格特征/好感度突破条件/关系突破条件/关系网变量\`
  - 外貌层：\`外貌描写/身材描写/衣着风格\`
  - 香闺秘档：\`胸部大小/乳头颜色/小穴颜色/后穴颜色/臀部大小/私密特质/私密总描述/子宫/是否处女/初夜字段/次数统计\`
- \`关系网变量\` 必须是关系边数组，且不少于 2 条。

## 7) 女主剧情规划开启时的额外格式约束
- 命令顺序需体现：
  - 即时清理无用内容 -> 初始化/创建 -> 触发条件同步 -> 状态推进 -> 触发后清理 -> 镜头拉回
- 当女主阶段目标/突破条件变化时，需在思考字段中明确“剧情钩子同步任务”，避免触发条件脱节。

## 8) shortTerm / action_options 规范
- \`shortTerm\`：<=100 字，只写剧情概况，不写判定结构和命令语句。
- \`action_options\`（开启时）：
  - 3-5 条纯字符串动作
  - 必须当下可执行
  - 禁止剧透、禁止结果导向、禁止替玩家执行

## 9) 常见错误禁止清单
- 禁止输出多个 JSON 对象，或 JSON 外附加说明。
- 禁止缺字段、乱序字段、缺 \`<thinking>\` 包裹。
- 禁止命令写入禁用路径或不存在父路径。
- 禁止“正文未发生命令已落地”与“正文已发生命令缺失”。
- 禁止跨层级移动只改 \`具体地点\`。
- 禁止重要女性角色更新后仍缺核心字段。

## 10) 最小完整示例（简短）
\`\`\`json
{
  "t_input": "<thinking>结论:先侦察后接触｜依据:玩家输入含打探且未授权强攻</thinking>",
  "t_plan": "<thinking>结论:推进线索支线并留主线钩子｜依据:当前章节冲突未到决战</thinking>",
  "t_state": "<thinking>结论:可执行低耗行动｜依据:精力可用、位置在集市、非战斗中</thinking>",
  "t_branch": "<thinking>A潜行跟踪 B公开询问 C先撤离；结论:选B｜依据:信息密度高且风险可控</thinking>",
  "t_precheck": "<thinking>结论:需更新时间+写NPC记忆+轻微精力消耗｜依据:对话打探已发生</thinking>",
  "logs": [
    { "sender": "旁白", "text": "..." },
    { "sender": "角色名", "text": "..." },
    { "sender": "【判定】", "text": "行动名称｜成功｜触发对象 玩家:玩家名称｜判定值 62/难度 48｜基础 +20 (属性)｜环境 +8 (地形)｜状态 +4 (专注)｜幸运 +30" }
  ],
  "t_logcheck": "<thinking>结论:叙事与选用分支一致｜依据:未越权代写玩家</thinking>",
  "t_var": "<thinking>结论:时间前进+精力-8+线索变量更新｜依据:执行了打探流程</thinking>",
  "t_npc": "<thinking>结论:线人在场并应写记忆｜依据:发生有效互动</thinking>",
  "t_cmd": "<thinking>结论:创建无需；直接修改现有字段｜依据:目标索引均存在</thinking>",
  "t_audit": "<thinking>结论:补一条剧情变量命令｜依据:主线钩子已触发</thinking>",
  "t_fix": "<thinking>结论:删除一条越界候选命令并改为合法路径</thinking>",
  "tavern_commands": [
    { "action": "set", "key": "gameState.环境.时间", "value": "317:03:16:10:20" }
  ],
  "t_mem": "<thinking>结论:记“打探成功+线索落地+下步追查”</thinking>",
  "shortTerm": "玩家在茶棚与线人接触，确认目标去向，场内局势从观望转入可追踪阶段。",
  "t_opts": "<thinking>结论:保守/中立/积极各给1条，避免剧透</thinking>",
  "action_options": ["继续追查目标路径", "向线人追问细节", "暂离现场规避耳目"]
}
\`\`\`

</多重思考输出格式>
`.trim(),
    类型: '核心设定',
    启用: false
};
