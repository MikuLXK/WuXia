import { 提示词结构 } from '../../types';

export const 核心_输出格式_多重思考: 提示词结构 = {
    id: 'core_format_multi',
    标题: '输出格式 - 多重思考',
    内容: `
<多重思考输出格式>
# 【多重思考版输出格式 | 全量对齐普通格式拆分版】

## 0) 标签协议硬约束
- 仅输出 1 组标签协议文本；禁止标签外解释文本。
- 输出顺序固定，不得乱序：
  1. \`<thinking>...</thinking>\`（内部分段按 \`t_input -> t_plan -> ... -> t_opts\` 顺序组织）
  2. \`<正文>...</正文>\`
  3. \`<短期记忆>...</短期记忆>\`
  4. \`<命令>...</命令>\`
  5. \`<disclaimer>...</disclaimer>\`（开关开启时）
  6. \`<行动选项>...</行动选项>\`（开关开启时）
- 每个 \`t_*\` 分段都必须写在 \`<thinking>\` 内，且必须使用 \`<thinking>...</thinking>\` 包裹思考正文。
- \`<短期记忆>\` 必须是 100 字内摘要；\`<正文>\` 与 \`<命令>\` 必须分别为独立标签块。

## 1) t_* 思考分段最小信息密度（强制）
- \`t_input\`：玩家目标/边界/禁止项 + [用户指令]解析结论。
- \`t_plan\`：回合类型、推进目标、冲突强度、收束点。
- \`t_state\`：关键状态快照（时间地点、资源、剧情门槛、战斗态、风险）。
- \`t_branch\`：A/B/C 三分支 + 选用理由 + 放弃理由。
- \`t_precheck\`：候选命令预演（前值->后值 + 合法/非法 + 理由），并包含时间推进、剧情钩子、世界动态、NPC 在场预检。
- \`t_logcheck\`：正文一致性/越权/因果复核。
- \`t_var\`：正文事实 -> 变量变化映射清单。
- \`t_npc\`：在场集合、入离场、记忆/关系同步、重要女性完整性复核。
- \`t_cmd\`：最终命令执行顺序方案（创建->修改->清理，及特殊模块顺序）。
- \`t_audit\`：路径、类型、索引、格式、上限下限、叙事一致性总审计。
- \`t_fix\`：删除非法候选并给出修正后可执行方向。
- \`t_mem\`：短期记忆提炼策略（行动-结果-影响-未决点）。
- \`t_opts\`：选项筛选逻辑（可执行、非剧透、互补）。

## 2) <正文> 标签规范（对齐普通格式）
- 正文只能使用一个 \`<正文>\` 标签（统一容器）；禁止拆出平行正文块。
- 正文逐行结构固定：
  - \`【旁白】...\`
  - \`【角色名】...\`
  - \`【判定】行动名称｜结果｜触发对象 玩家:姓名 或 NPC:姓名｜判定值 X/难度 Y｜基础 B (说明)｜环境 E (说明)｜状态 S (说明)｜幸运 L\`
- 旁白/对话分离：
  - \`【旁白】\` 只写叙述，不得含直接引语（\`“...”\`/\`「...」\`/\`『...』\`）或“角色名+冒号”发言格式。
  - \`【角色名】\` 只写台词，不得夹带动作与环境叙述。
  - **角色发言必须另起一行**：每次发言必须单独一行，不得与旁白共享同一行。
  - 若出现“某人说道 + 引号内容”，必须拆成“旁白动作”与“角色台词”两行，禁止混写。
  - **旁白段落必须长短错落**：每回合至少 1 条中长旁白（建议 2-4 句，约 80-180 字），其余可用 20-70 字短旁白调节节奏。
  - 禁止“一句话一个【旁白】”的机械切分；同一时空连续动作必须合并为完整段落后再切分。
  - 违规示例（禁止）：\`【旁白】店小二赔笑道：“客官里边请。”\`
  - 合规示例（必须）：
    - \`【旁白】店小二赔着笑，侧身让出门道。\`
    - \`【店小二】客官里边请。\`
- 正文中未发生事实，不得在命令里提前落地。

## 3) <命令> 标签规范（对齐普通格式）
- \`<命令>\` 中每行一个变量命令，固定格式：
  - \`add|set|push|delete gameState.xxx (= 值)\`
- 仅允许操作：\`add/set/push/delete\`。
- 可写域：\`gameState.角色/环境/社交/世界/战斗/剧情/女主剧情规划(开启时)\`
- 禁写：\`gameState.背包\`
- 命令顺序强制：创建 -> 修改 -> 清理。
- 任何非法候选命令不得进入最终命令块。

## 4) 必须生成指令的场景（多重思考版）
- 时间推进/跨日：
  - \`set gameState.环境.时间\`（\`YYYY:MM:DD:HH:MM\`）
  - 跨日再 \`set gameState.环境.游戏天数\`
- 位置变化：
  - 同小地点微移动可仅改 \`具体地点\`
  - 跨层级移动必须联动 \`大地点/中地点/小地点/具体地点\`
- 物品获得（叙事明确拾取/装入）：
  - \`push gameState.角色.物品列表\`（完整对象）
  - 同步 \`当前容器ID\`；装备态同步 \`当前装备部位\`
  - 同步容器 \`当前已用空间\`（及必要的容器占用变化）
- 掉落未拾取：
  - 只写 \`<正文>\`，不写物品归属命令
- 物品消耗：
  - 单件建模：\`delete gameState.角色.物品列表[i]\`
  - 具备数量字段时：先 \`add 数量 -1\`，归零后再删
- 战斗/体力/受伤：
  - 部位血量与精力变动需落地 \`add\`
- 社交/NPC：
  - 好感、关系、在场、队伍状态变化需落地
  - 有效互动后写入 \`gameState.社交[i].记忆\`
  - 新建档必须先查重并满足建档条件
- 剧情推进：
  - \`剧情变量\`、\`待触发事件\`、\`历史卷宗\` 等变化需按事实落地

## 5) 前置依赖与合法性检查（强制）
- 父对象存在性：不存在先创建后修改。
- 索引合法：\`社交[i]\`、\`物品列表[i]\` 必须来自确认映射。
- 类型匹配：\`add\` 数值、\`push\` 数组、\`set\` 类型兼容。
- 下限/上限检查：扣减不低于下限，增加不超过上限字段。
- 容量检查：容器存在、单件大小合法、总容量不超限。
- 时间格式检查：所有时间字段必须标准格式。
- 叙事一致性：正文发生才可下命令，正文未发生不得落地。

## 6) NPC 与重要女性门禁（强制）
- 必须先构建“本回合在场角色集合”后再写 NPC 互动命令。
- 未在场 NPC 默认不得写本场景直接互动记忆/好感（远程互动需有明确依据）。
- 命中 \`性别=女 且 是否主要角色=true\` 时，命令必须覆盖缺项补齐：
  - 关系驱动：\`核心性格特征/好感度突破条件/关系突破条件/关系网变量\`
  - 外貌层：\`外貌描写/身材描写/衣着风格\`
  - 香闺秘档：\`胸部大小/乳头颜色/小穴颜色/后穴颜色/臀部大小/私密特质/私密总描述/子宫/是否处女/初夜字段/次数统计\`
- \`关系网变量\` 必须是关系边数组，且不少于 2 条。

## 7) 女主剧情规划开启时的额外格式约束
- 命令顺序需体现：
  - 即时清理无用内容 -> 初始化/创建 -> 触发条件同步 -> 状态推进 -> 触发后清理 -> 镜头拉回
- 当女主阶段目标/突破条件变化时，需在思考字段中明确“剧情钩子同步任务”，避免触发条件脱节。

## 8) <短期记忆> / <行动选项> 规范
- \`<短期记忆>\`：<=100 字，只写剧情概况，不写判定结构和命令语句。
- \`<行动选项>\`（开启时）：
  - 3-5 条纯字符串动作
  - 必须当下可执行
  - 禁止剧透、禁止结果导向、禁止替玩家执行

## 9) 常见错误禁止清单
- 禁止输出多个正文/命令标签块，或标签外附加说明。
- 禁止缺标签、标签乱序、缺 \`<thinking>\` 包裹。
- 禁止命令写入禁用路径或不存在父路径。
- 禁止“正文未发生命令已落地”与“正文已发生命令缺失”。
- 禁止跨层级移动只改 \`具体地点\`。
- 禁止重要女性角色更新后仍缺核心字段。

## 10) 最小完整示例（简短）
\`\`\`text
<thinking>
[t_input] 结论:先侦察后接触｜依据:玩家输入含打探且未授权强攻
[t_plan] 结论:推进线索支线并留主线钩子｜依据:当前章节冲突未到决战
[t_state] 结论:可执行低耗行动｜依据:精力可用、位置在集市、非战斗中
...
</thinking>
<正文>
【旁白】你在茶棚压低斗笠边，先扫过四角的耳目动静。
【线人】南巷口那人今晨去了渡口，随行只有一名短刀护卫。
【判定】打探目标去向｜成功｜触发对象 玩家:玩家名称｜判定值 62/难度 48｜基础 +20 (洞察)｜环境 +8 (熟悉地形)｜状态 +4 (专注)｜幸运 +30
</正文>
<短期记忆>玩家与线人完成接触并确认目标去向，场内局势从观望转入可追踪阶段。</短期记忆>
<命令>
set gameState.环境.时间 = "317:03:16:10:20"
add gameState.角色.当前精力 = -8
push gameState.社交[3].记忆 = {"内容":"茶棚打探确认目标去向","时间":"317:03:16:10:20"}
</命令>
<行动选项>
- 继续追查目标路径
- 向线人追问随行护卫底细
- 暂离现场规避耳目
</行动选项>
\`\`\`

</多重思考输出格式>
`.trim(),
    类型: '核心设定',
    启用: false
};
