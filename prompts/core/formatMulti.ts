import { 提示词结构 } from '../../types';

export const 核心_输出格式_多重思考: 提示词结构 = {
    id: 'core_format_multi',
    标题: '输出格式 - 多重思考',
    内容: `
<多重思考输出格式>
# 【多重思考版输出格式 | 强约束完整版】

## 1) JSON 总规则
- 只输出 1 个合法 JSON 对象，不输出任何 JSON 外解释文字。
- 字段顺序固定，不可乱序、不可漏关键字段：
  1. \`t_input\`
  2. \`t_plan\`
  3. \`t_state\`
  4. \`t_branch\`
  5. \`t_precheck\`
  6. \`logs\`
  7. \`t_logcheck\`
  8. \`t_var\`
  9. \`t_npc\`
  10. \`t_cmd\`
  11. \`t_audit\`
  12. \`t_fix\`
  13. \`tavern_commands\`
  14. \`t_mem\`
  15. \`shortTerm\`
  16. \`t_opts\`
  17. \`action_options\`（开关开启时）
- 每个 \`t_*\` 思考字段必须是字符串，且用 \`<thinking>...</thinking>\` 包裹。

## 2) 思考字段最小内容标准
- \`t_input\`：玩家输入的目标/限制/禁止项。
- \`t_plan\`：本回合推进策略与节奏（低压/中压/高压）。
- \`t_state\`：当前关键状态（时间地点、资源、剧情门槛、战斗态）。
- \`t_branch\`：A/B/C 三分支 + 取舍结论。
- \`t_precheck\`：候选命令预演（前值->后值 + 合法性结论）。
- \`t_logcheck\`：正文一致性与越权检查。
- \`t_var\`：正文对应变量变化总表述。
- \`t_npc\`：NPC在场、关系、记忆与队友状态同步检查；命中重要女性角色时，必须补充完整档案缺项检查。
- \`t_cmd\`：最终命令结构规划（创建/修改/清理顺序）。
- \`t_audit\`：查缺补漏（路径/类型/索引/时间格式）。
- \`t_fix\`：删除非法命令并说明纠正方向。
- \`t_mem\`：短期记忆压缩策略。
- \`t_opts\`：快速选项筛选逻辑（可执行、非剧透、互斥度适中）。

## 3) 思考字段长度控制
- 每个思考字段建议 1-3 行，优先“结论｜依据”。
- 总体目标：简洁但完整，避免空泛口号和跨字段重复。
- 若出现冲突，优先保留“可执行判断 + 风险说明 + 合法性结论”。

## 4) logs 字段规范
- \`logs\` 必须为数组。
- 每个元素结构固定：
  - \`{"sender":"旁白|角色名|【判定】|【NSFW判定】","text":"..."}\`
- 旁白与台词必须分离：
  - \`sender="旁白"\`：叙述性文字，不含直接台词格式。
  - \`sender="角色名"\`：只写台词，不混动作叙述。
- 判定必须单独一条 logs，不得塞入 \`shortTerm\`。
- logs 内发生的事实才允许命令落地；未发生不得下命令。

## 5) tavern_commands 字段规范
- \`tavern_commands\` 必须为数组；每条命令结构：
  - \`{"action":"add|set|push|delete","key":"gameState.xxx","value":...}\`
- 命令必须满足：
  - 路径在可写域内
  - 父对象存在（必要时先创建）
  - 数组索引有效
  - 类型匹配
  - 时间格式正确
  - 与 logs 叙事一致
- 命令顺序：创建 -> 修改 -> 清理。
- 最终数组中不得保留任何“已判定非法”的候选命令。
- 重要女性完整性门禁（强制）：
  - 若 \`性别=女 且 是否主要角色=true\`，\`tavern_commands\` 必须确保以下字段完整：
    1) 关系驱动：\`核心性格特征/好感度突破条件/关系突破条件/关系网变量\`
    2) 外貌层：\`外貌描写/身材描写/衣着风格\`
    3) 香闺秘档：\`胸部大小/乳头颜色/小穴颜色/后穴颜色/臀部大小/私密特质/私密总描述/子宫/是否处女/初夜字段/次数统计\`
  - \`关系网变量\` 必须是关系边数组（对象姓名/关系/备注），且不少于 2 条。

## 6) shortTerm / action_options 规范
- \`shortTerm\`：<=100 字，概括“行动、结果、关键变化、未决点”。
- \`action_options\`（开启时）：
  - 3-5 条字符串
  - 必须是“当下可执行动作”
  - 禁止提前给结果、禁止剧透终局

## 7) 常见错误禁止清单
- 禁止输出多个 JSON 对象或在 JSON 外附加说明。
- 禁止遗漏 \`logs\` 或 \`tavern_commands\`。
- 禁止把 thoughts 写成普通文本而不使用 \`<thinking>\` 包裹。
- 禁止命令写入禁用路径（如 \`gameState.背包\`）。
- 禁止“正文没发生、命令已落地”或“正文发生、命令缺失”。
- 禁止重要女性角色建档或更新后仍缺失关系网/外貌/香闺秘档关键字段。

## 8) 最小完整示例（简短）
\`\`\`json
{
  "t_input": "<thinking>结论:先侦察后接触｜依据:玩家输入含打探且未授权强攻</thinking>",
  "t_plan": "<thinking>结论:推进线索支线并留主线钩子｜依据:当前章节冲突未到决战</thinking>",
  "t_state": "<thinking>结论:可执行低耗行动｜依据:精力可用、位置在集市、非战斗中</thinking>",
  "t_branch": "<thinking>A潜行跟踪 B公开询问 C先撤离；结论:选B｜依据:信息密度高且风险可控</thinking>",
  "t_precheck": "<thinking>结论:需更新时间+写NPC记忆+轻微精力消耗｜依据:对话打探已发生</thinking>",
  "logs": [
    { "sender": "旁白", "text": "..." },
    { "sender": "角色名", "text": "..." },
    { "sender": "【判定】", "text": "行动名称｜成功｜触发对象 玩家:玩家名称｜判定值 62/难度 48｜基础 +20 (属性)｜环境 +8 (地形)｜状态 +4 (专注)｜幸运 +30" }
  ],
  "t_logcheck": "<thinking>结论:叙事与选用分支一致｜依据:未越权代写玩家</thinking>",
  "t_var": "<thinking>结论:时间前进+精力-8+线索变量更新｜依据:执行了打探流程</thinking>",
  "t_npc": "<thinking>结论:线人在场并应写记忆｜依据:发生有效互动</thinking>",
  "t_cmd": "<thinking>结论:创建无需；直接修改现有字段｜依据:目标索引均存在</thinking>",
  "t_audit": "<thinking>结论:补一条剧情变量命令｜依据:主线钩子已触发</thinking>",
  "t_fix": "<thinking>结论:删除一条越界候选命令并改为合法路径</thinking>",
  "tavern_commands": [
    { "action": "set", "key": "gameState.环境.时间", "value": "317:03:16:10:20" }
  ],
  "t_mem": "<thinking>结论:记“打探成功+线索落地+下步追查”</thinking>",
  "shortTerm": "玩家在茶棚与线人接触，确认目标去向，场内局势从观望转入可追踪阶段。",
  "t_opts": "<thinking>结论:保守/中立/积极各给1条，避免剧透</thinking>",
  "action_options": ["继续追查目标路径", "向线人追问细节", "暂离现场规避耳目"]
}
\`\`\`

</多重思考输出格式>
`.trim(),
    类型: '核心设定',
    启用: false
};
