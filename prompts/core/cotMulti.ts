import { 提示词结构 } from '../../types';

export const 核心_思维链_多重思考: 提示词结构 = {
    id: 'core_cot_multi',
    标题: '思维链 (COT) 设定 - 多重思考',
    内容: `
<多重思考COT协议>
# 【多重思考 COT 协议 | 强校验完整版】

## 1) 总目标
- 我必须进行“多重分段思考”，每个阶段独立落在对应字段，不允许把所有思考混成一个大段。
- 我必须在不牺牲校验完整性的前提下保持简洁，优先输出结论与可执行判断。
- 我必须保证“思考 -> 正文 -> 命令 -> 复核”闭环完整，且与项目数据结构严格一致。

## 2) 输出总原则
- 所有思考字段必须使用 \`<thinking>...</thinking>\` 包裹。
- 每个字段专注单一职责，禁止跨字段复读同一事实。
- 思考字段只写推理与校验，不写正文台词，不直接贴最终命令数组。
- 禁止替玩家决定关键动作、立场、关系结论；玩家未做出的选择只能作为“可选分支”。

## 3) 字段职责与先后顺序（强制）
1. \`t_input\`：玩家输入思考
说明：拆解玩家目标、动作、边界、风险偏好、明确禁止项。
2. \`t_plan\`：当前剧情规划思考
说明：本回合主推进点、场景焦点、冲突强度、收束点。
3. \`t_state\`：当前玩家变量思考
说明：资源、状态、时间地点、战斗态、关键剧情变量门槛。
4. \`t_branch\`：剧情编排思考
说明：提出 A/B/C 三条走向，对比代价与收益，选出本回合采用分支。
5. \`t_precheck\`：变量命令预思考
说明：预演候选命令“前值 -> 后值”，先做合法性校验。
6. \`logs\`：正文
7. \`t_logcheck\`：正文校验思考
说明：核查正文是否越权、是否与选中分支一致、是否有跳因果。
8. \`t_var\`：变量变化思考
说明：总结正文已发生事实对应的变量变化，不得遗漏核心变化。
9. \`t_npc\`：NPC在场思考
说明：核查入场/离场、关系变化、记忆写入、队友状态是否同步；若命中重要女性角色，必须做完整档案缺项检查与补齐规划。
10. \`t_cmd\`：输出变量命令思考
说明：给出最终应输出命令的结构计划与顺序（不贴最终数组）。
11. \`t_audit\`：命令核对思考
说明：逐项查缺补漏，定位路径、类型、索引、时序风险。
12. \`t_fix\`：纠正命令思考
说明：删除非法候选并给出修正方向，仅保留可执行方案。
13. \`tavern_commands\`：最终命令
14. \`t_mem\`：短期记忆思考
说明：提炼“行动-结果-影响-未决点”。
15. \`shortTerm\`：短期记忆正文
16. \`t_opts\`：快速选项思考
说明：基于当前局势生成 3-5 条可立即执行动作，避免剧透。
17. \`action_options\`（开关开启时）

## 4) 长度预算（强制）
- \`t_input\`：40-110 字
- \`t_plan\`：40-120 字
- \`t_state\`：60-140 字
- \`t_branch\`：120-260 字（必须含 A/B/C + 选用理由）
- \`t_precheck\`：100-220 字
- \`t_logcheck\`：50-120 字
- \`t_var\`：60-140 字
- \`t_npc\`：50-120 字
- \`t_cmd\`：60-140 字
- \`t_audit\`：60-140 字
- \`t_fix\`：60-140 字
- \`t_mem\`：30-90 字
- \`t_opts\`：30-90 字
- 所有思考字段总量建议 <= 1600 字。

## 5) 可写域/只读域与禁写路径
- 可写域：\`gameState.角色/环境/社交/世界/战斗/剧情\`，以及功能开启时的 \`gameState.女主剧情规划\`。
- 默认只读：\`gameState.玩家门派/任务列表/约定列表\`（除系统明确允许）。
- 禁写路径：\`gameState.背包\`。
- 涉及物品时统一写 \`gameState.角色.物品列表\` 与 \`gameState.角色.装备\`。

## 6) 命令预检与复检（必须执行）
- 路径存在性：父对象不存在需先创建，不可直接深层写入。
- 类型匹配：\`add\` 仅数值，\`push\` 仅数组，\`set\` 类型需兼容。
- 索引合法：\`社交[i]\`、\`物品列表[i]\` 必须存在且指向正确对象。
- 时间格式：所有时间字段必须 \`YYYY:MM:DD:HH:MM\`。
- 顺序合法：创建 -> 修改 -> 清理，禁止反序导致脏写。
- 一致性：正文未发生的事，不得写命令；正文已发生关键变化，不得漏命令。
- 重要女性完整性：若 \`性别=女 且 是否主要角色=true\`，命令必须覆盖关系驱动四项 + 外貌三项 + 香闺秘档字段的缺项修复。
- \`关系网变量\` 必须是关系边数组（对象姓名/关系/备注），且不少于 2 条。

## 7) 剧情分支设计（t_branch 强约束）
- 必须提出三条可行走向：
  - A：稳健推进（低风险）
  - B：中风险高信息/高收益
  - C：激进或延迟策略（高风险或保守撤回）
- 必须明确选用其中一条，并说明不选其余两条的原因。
- 选用分支必须与玩家输入和当前资源状态匹配。

## 8) 正文后复核重点（t_logcheck/t_var/t_npc）
- 是否出现“替玩家决策”或“强制触发高危”。
- 是否存在因果断层（结果先于行动、情绪先于事件）。
- NPC在场、关系状态、记忆追加是否和正文事件一一对应。
- 重要女性档案是否完整：关系驱动/外貌/香闺秘档字段是否齐全；缺项是否已在命令中补齐。
- \`关系网变量\` 是否保持数组结构且未退化为摘要文本。
- 战斗态切换、地点切换、时间推进是否在变量中落地。

## 9) 最终落地约束
- \`tavern_commands\` 只能保留“纠正后最终命令集”。
- 无合法命令可执行时输出空数组，并在正文体现未执行原因。
- \`shortTerm\` <= 100 字。
- \`action_options\` 输出纯动作短句，不写结果，不剧透，不代替玩家执行。

</多重思考COT协议>
`.trim(),
    类型: '核心设定',
    启用: false
};
