import { 提示词结构 } from '../../types';

export const 核心_思维链_多重思考: 提示词结构 = {
    id: 'core_cot_multi',
    标题: '思维链 (COT) 设定 - 多重思考',
    内容: `
<多重思考COT协议>
# 【多重思考 COT 协议 | 全量拆分版】

## 0) 目标与边界
- 本协议将普通 COT 的完整检查流拆分到 \`t_*\` 字段，保持“输入解析 -> 方案 -> 预检 -> 正文 -> 复核 -> 修正 -> 落地”闭环。
- 所有思考字段只写推理、校验、取舍，不写正文台词，不直接贴最终 \`tavern_commands\` 数组。
- 禁止替玩家做关键决定；玩家未明确授权的动作只能作为候选分支。
- 所有判断仅可依据上下文已给事实；未出现的字段/事实一律视为未知。

## 1) 上下文读取基准（仅以下内容可作为事实）
- \`【世界观提示词】\`
- \`【离场NPC档案】\` (此为 \`gameState.社交\` 的子集视图，索引通用)
- \`【叙事/规则提示词】\`
- \`<时间推进法则>...</时间推进法则>\`
- \`<物品重量参考>...</物品重量参考>\`
- \`【长期记忆】\` / \`【中期记忆】\` / \`【短期记忆】\`
- \`【当前场景NPC档案】\` (此为 \`gameState.社交\` 的子集视图，索引通用)
- \`【游戏设置】\`
- \`【剧情安排】\`
- \`【世界】\` / \`【当前环境】\` / \`【角色】\` / \`【战斗】\` / \`【玩家门派】\` / \`【任务列表】\` / \`【约定列表】\`
- \`【即时剧情回顾 (Script)】\`
- \`<玩家输入>...</玩家输入>\`
- 额外要求提示词（若存在）

## 2) 可写域、只读域与禁写路径
- 可写域：\`gameState.角色\` / \`gameState.环境\` / \`gameState.社交\` / \`gameState.世界\` / \`gameState.战斗\` / \`gameState.剧情\` / \`gameState.女主剧情规划(开启时)\`
- 默认只读：\`gameState.玩家门派\` / \`gameState.任务列表\` / \`gameState.约定列表\`
- 禁写：\`gameState.背包\`
- 物品相关统一使用 \`gameState.角色.物品列表\` 与 \`gameState.角色.装备\`

## 3) 字段顺序与职责（强制）
1. \`t_input\`
2. \`t_plan\`
3. \`t_state\`
4. \`t_branch\`
5. \`t_precheck\`
6. \`logs\`
7. \`t_logcheck\`
8. \`t_var\`
9. \`t_npc\`
10. \`t_cmd\`
11. \`t_audit\`
12. \`t_fix\`
13. \`tavern_commands\`
14. \`t_mem\`
15. \`shortTerm\`
16. \`t_opts\`
17. \`action_options\`（开关开启时）

## 4) 各字段详细要求（把普通 COT 拆分落位）

### A. \`t_input\`（输入解析）
- 必须拆解：意图 / 动作 / 范围 / 停止条件。
- 若存在 \`[用户指令]...[/用户指令]\`，逐条抽取为最高优先目标。
- 明确本回合行动上限：玩家没说的事不得自动补做、不得链式扩展。

### B. \`t_plan\`（回合规划）
- 先判定回合类型：悠闲日常 / 探索调查 / 主线推进 / 冲突对抗。
- 给出本回合推进目标：主推进点、冲突强度、收束点、是否需要主线更新。
- 若开启女主剧情规划：补充“焦点女主与群像承接策略”，并标注是否需要镜头拉回。

### C. \`t_state\`（事实快照）
- 角色：精力/饱腹/口渴/部位血量/六维/金钱/装备/物品/功法。
- 环境：时间、天气、环境变量、大/中/小/具体地点；地点解析顺序固定为大->中->小->具体。
- 战斗：是否战斗中、敌方状态。
- 剧情：当前章节、结束条件、下一章预告、历史卷宗、近期/中期/长期规划、待触发事件、剧情变量。
- 世界：活跃 NPC、地图、建筑、进行中事件、已结算事件、江湖史册。
- 列出风险与不确定性：资源不足、条件不满足、索引未知、时间冲突、位置冲突。

### D. \`t_branch\`（分支编排）
- 必须提出 A/B/C 三分支：
  - A：低风险稳健推进
  - B：中风险高信息或高收益
  - C：激进推进或保守撤回
- 明确选用 1 条，并说明放弃其余两条原因。
- 选中分支必须与玩家输入、当前资源、当前风险匹配。

### E. \`t_precheck\`（预检，承接普通 COT 的前置校验）
- 时间推进分析（强制）：
  - 依据 \`<时间推进法则>\` 估算本回合耗时；
  - 同回合多段行动需合并耗时；
  - 预规划 \`set gameState.环境.时间\`，跨日再加 \`set gameState.环境.游戏天数\`。
- 世界动态预检（强制）：
  - 判断是否需要补齐/结算/清理世界事件；
  - 到期事件需迁移结算并清理无价值项；
  - 校验事件对象完整性与类型合法性。
- 剧情钩子预检（强制）：
  - 校验 \`待触发事件\` 字段完整；
  - 当前时间 >= 失效时间的钩子必须规划清理；
  - 维持规模目标 1~3 条。
- NPC 在场预检（强制）：
  - **NPC档案解读**：\`【当前场景NPC档案】\` 和 \`【离场NPC档案】\` 均为 \`gameState.社交\` 的视图，共享索引。档案以 \`\"[索引] 姓名\": { ... }\` 的JSON对象形式提供。其中，\`是否主要角色: true\` 表示该角色有更详细的数据；\`false\` 表示只有基础数据或纯文本摘要。
  - **索引映射**：必须先从这两个档案的 \`[索引]\` 建立“姓名/id -> 索引”的映射，所有 \`gameState.社交[i]\` 操作都必须基于此映射，严禁按名字臆测索引 \`i\`。
  - **在场集合构建**：以 \`【当前场景NPC档案】\` 的所有条目为初始在场集合，再依据 logs 中的“入场/离场”语义进行增删。
  - **越权检查**：未在场角色默认不得写入本场景的直接互动记忆。
  - **命令顺序**：强制要求先用 \`set\` 命令纠偏 \`是否在场\` 状态，然后再写入记忆、好感或关系状态。
- 命令三检（强制）：
  - 支持性：\`add\` 仅数值，\`push\` 仅数组，\`set\` 类型兼容，\`delete\` 目标存在；
  - 下限：扣减后不得低于下限（默认 0）；
  - 上限：存在上限字段时不得超限。
- 容器专项三检（收纳时强制）：
  - 容器存在且含 \`容器属性\`；
  - 单件大小不超 \`最大单物大小\`；
  - 放入后不超 \`最大容量\`。
- 重要女性完整性门禁（强制）：
  - 命中 \`性别=女 且 是否主要角色=true\` 时，先检查关系驱动四项 + 外貌三项 + 香闺秘档字段；
  - 有缺项必须先补齐后推进；
  - \`关系网变量\` 必须为关系边数组且 >=2 条。
- 候选命令必须以“前值 -> 后值 + 合法/非法 + 理由”形式预演。

### F. \`logs\`（正文）
- 仅承载沉浸叙事、对话、结构化判定。
- 旁白与台词严格分离：旁白不含直接引语和“角色名:台词”格式；角色字段只写台词。
- 不得代写玩家未输入的关键动作/对白/决策。
- 判定行仅允许写在 logs，不得写进 \`shortTerm\`。

### G. \`t_logcheck\`（正文后第一层复核）
- 核对正文是否与选中分支一致，是否越权，是否跳因果。
- 若正文与预检冲突，必须标注改道策略，不得硬落地原候选命令。

### H. \`t_var\`（变量映射复核）
- 把“正文已发生事实”映射为“应落地变量变化清单”。
- 至少覆盖：时间、地点、资源、剧情钩子、战斗态、关键剧情变量。
- 禁止把未发生事实映射成成功变化。

### I. \`t_npc\`（NPC 专项复核）
- 核对入场/离场、在场集合、好感/关系、记忆写入与正文是否一一对应。
- 检查是否存在“未在场却写入直接互动记忆/好感”的越权命令。
- 若本回合新建档，校验建档条件、查重结果与 \`是否在场\` 初值一致性。
- 命中重要女性角色时复核缺项是否已补齐。

### J. \`t_cmd\`（命令结构规划）
- 输出“创建 -> 修改 -> 清理”的最终命令顺序方案。
- 地点命令遵守层级联动：
  - 同小地点微移动才可只改 \`具体地点\`；
  - 跨层级移动必须联动更新受影响字段。
- 若开启女主剧情规划，命令顺序按：
  - 即时清理无用内容 -> 初始化/创建 -> 触发条件同步 -> 状态推进 -> 触发后清理 -> 镜头拉回。

### K. \`t_audit\`（总审计）
- 逐项审计：路径合法性、类型、索引、时间格式、上/下限、叙事一致性。
- 必查：
  - 世界事件是否按“预计结束时间”维护；
  - 待触发事件过期项是否清理；
  - 三段剧情规划与变量状态是否一致；
  - 战斗结束是否同步 \`是否战斗中=false\` 与 \`敌方=null\`。

### L. \`t_fix\`（修正）
- 删除非法候选，给出改写后的可执行命令方向。
- 若无合法命令，明确输出 \`tavern_commands: []\`，并要求正文体现“条件不足未执行”。

### M. \`t_mem\` 与 \`shortTerm\`
- \`t_mem\`：提炼“行动-结果-影响-未决点”。
- \`shortTerm\`：100 字内剧情概况；禁止判定结构、时间戳列表、命令语句。

### N. \`t_opts\` 与 \`action_options\`
- 仅在开关开启时输出 \`action_options\`。
- 3-5 条纯动作短句；必须当下可执行；禁止剧透结果、禁止替玩家执行。

## 5) 长度预算（建议）
- \`t_input\`：40-120 字
- \`t_plan\`：60-160 字
- \`t_state\`：80-220 字
- \`t_branch\`：140-320 字（必须 A/B/C + 选用理由）
- \`t_precheck\`：180-380 字
- \`t_logcheck\`：60-160 字
- \`t_var\`：80-200 字
- \`t_npc\`：80-220 字
- \`t_cmd\`：80-200 字
- \`t_audit\`：100-240 字
- \`t_fix\`：80-200 字
- \`t_mem\`：30-100 字
- \`t_opts\`：30-100 字

## 6) 落地底线
- 所有 \`t_*\` 字段必须使用 \`<thinking>...</thinking>\` 包裹。
- 只输出单一 JSON 对象；不得在 JSON 外附加文本。
- \`tavern_commands\` 只能保留纠正后的最终合法命令集。

</多重思考COT协议>
`.trim(),
    类型: '核心设定',
    启用: false
};
