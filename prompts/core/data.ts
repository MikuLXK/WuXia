import { 提示词结构 } from '../../types';

export const 核心_数据格式: 提示词结构 = {
    id: 'core_data',
    标题: '数据格式',
    内容: `
<数据结构定义>
# 【数据结构定义】WuXia SaveData (V1.2 Chinese Native / Ultra Complete)

> 目标：定义本项目“可执行且完整”的状态树。AI 生成 \`tavern_commands\` 时，必须严格遵守本定义。
> 原则：先看路径是否存在，再看字段是否匹配，再看类型与边界。
> 示例引用约束：本文所有示例仅用于“结构说明”，严禁照搬任何示例中的名称、地点、势力、年份、数值。
> 时间铁律：所有“结构化时间字符串”字段统一格式为 \`YYYY:MM:DD:HH:MM\`（例：\`317:03:16:09:45\`）。
> 环境字段特例（强制）：\`gameState.环境.时间\` 必须是上述标准时间戳；\`gameState.环境.游戏天数\` 仅表示“第几日”（1/2/3...），不得写成年月日字符串。

## 0. 路径总则（CRITICAL）
1. 仅允许当前引擎可执行路径：
   - \`gameState.角色\`
   - \`gameState.环境\`
   - \`gameState.社交\`
   - \`gameState.世界\`
   - \`gameState.战斗\`
   - \`gameState.剧情\`
   - \`gameState.女主剧情规划\`（仅在“女主剧情规划功能”开启时写入）
2. 不可直接写入的顶层（当前引擎未实现命令路由）：
   - \`gameState.玩家门派\`、\`gameState.任务列表\`、\`gameState.约定列表\`
   - 上述结构在上下文中存在，但默认视为只读域。
3. 禁止使用不存在路径：
   - \`gameState.背包\`（请改用 \`gameState.角色.物品列表\`）

## 1. 环境结构（gameState.环境）完整定义
- \`时间\`: String（**必须**为 \`YYYY:MM:DD:HH:MM\`，例如 \`317:03:16:09:45\`）
- \`大地点\`: String
- \`中地点\`: String
- \`小地点\`: String
- \`具体地点\`: String（只写“小地点内部的具体位置”，禁止重复包含小地点名称）
- \`节日\`: { \`名称\`: String, \`简介\`: String, \`效果\`: String } | null
- \`天气\`: { \`天气\`: String, \`结束日期\`: String(YYYY:MM:DD:HH:MM) }
- \`环境变量\`: { \`名称\`: String(四字), \`描述\`: String, \`效果\`: String } | null
- \`游戏天数\`: Number（仅“第几日”日序，如 1/2/3...；**不得**写 \`YYYY:MM:DD\`）

### 1.1 时间推进约束（CRITICAL）
- 跨分钟/小时/跨日推进时，优先 \`set gameState.环境.时间\` 到新时间戳。
- 同步更新 \`gameState.环境.游戏天数\` 为“第几日”计数（通常在跨日时 +1）。
- 禁止只改 \`日期\` 不改 \`时间\`，也禁止把 \`日期\` 当年月日使用。
- 具体推进粒度与防拖慢规则以 \`core_time_progress\`（时间推进法则）为准。

## 2. 角色结构（gameState.角色）完整定义
### 2.1 基础信息
- \`姓名\`: String
- \`性别\`: "男" | "女"
- \`年龄\`: Number
- \`出生日期\`: String（N月N日，例如 3月1日）
- \`外貌\`: String（角色外貌/气质/常见穿着）
- \`称号\`: String
- \`境界\`: String
- \`天赋列表\`: Array<{ 名称: String, 描述: String, 效果: String }>
- \`出身背景\`: { 名称: String, 描述: String, 效果: String }

### 2.2 门派归属
- \`所属门派ID\`: String
- \`门派职位\`: String
- \`门派贡献\`: Number

### 2.3 生存/负重
- \`当前精力\`: Number
- \`最大精力\`: Number
- \`当前饱腹\`: Number
- \`最大饱腹\`: Number
- \`当前口渴\`: Number
- \`最大口渴\`: Number
- \`当前负重\`: Number
- \`最大负重\`: Number

### 2.3.1 金钱（统一货币结构）
- \`金钱\`: {
  \`金元宝\`: Number,
  \`银子\`: Number,
  \`铜钱\`: Number
}
- 三项必须为非负数；建议使用整数。
- 玩家现金仅允许写入 \`gameState.角色.金钱\`，禁止臆造 \`gameState.金币\` 或同级散字段。

### 2.4 六维
- \`力量\`: Number
- \`敏捷\`: Number
- \`体质\`: Number
- \`根骨\`: Number
- \`悟性\`: Number
- \`福源\`: Number

### 2.5 部位生命（扁平化）
- \`头部当前血量\` / \`头部最大血量\` / \`头部状态\`
- \`胸部当前血量\` / \`胸部最大血量\` / \`胸部状态\`
- \`腹部当前血量\` / \`腹部最大血量\` / \`腹部状态\`
- \`左手当前血量\` / \`左手最大血量\` / \`左手状态\`
- \`右手当前血量\` / \`右手最大血量\` / \`右手状态\`
- \`左腿当前血量\` / \`左腿最大血量\` / \`左腿状态\`
- \`右腿当前血量\` / \`右腿最大血量\` / \`右腿状态\`

### 2.6 装备
- \`装备\`: {
  \`头部\`, \`胸部\`, \`腿部\`, \`手部\`, \`足部\`,
  \`主武器\`, \`副武器\`, \`暗器\`, \`背部\`, \`腰部\`, \`坐骑\`
}

### 2.7 物品列表（Array<游戏物品>）完整字段族
- 统一基础字段：
  - \`ID, 名称, 描述, 类型, 品质, 重量(单位:斤), 占用空间, 价值, 当前耐久, 最大耐久, 词条列表, 当前容器ID?, 当前装备部位?\`
- 可选结构：
  - \`容器属性\`: { 最大容量, 当前已用空间, 最大单物大小, 减重比例 }
  - 武器扩展：\`武器子类, 最小攻击, 最大攻击, 攻速修正, 格挡率\`
  - 防具扩展：\`装备位置, 覆盖部位, 物理防御, 内功防御\`
  - 秘籍扩展：\`对应技能名, 学习要求, 研读进度, 是否消耗\`
  - 消耗品扩展：\`使用效果[], 毒性\`

### 2.7.1 容器优先收纳铁律（CRITICAL）
- \`gameState.角色.物品列表\` 是“物品总表”，不是“无容器裸堆区”。
- 除“当前穿戴/手持剧情态”外，物品入包必须有明确 \`当前容器ID\`。
- 每个已收纳物品必须写 \`当前容器ID\`，用于唯一定位其所在容器。
- 已装备物品必须同时满足：\`当前装备部位\` 有值，且 \`当前容器ID = 当前装备部位\`（如 \`主武器\`）。
- 容器可来自：
  - 纯容器物品（如布袋、背篓、锦囊）
  - 带口袋的防具/衣物（防具也可带 \`容器属性\`）
- 禁止“只 push 到物品列表，不写容器归属”。
- \`gameState.角色.装备\` 允许写名称或ID，但引用到的装备必须在 \`物品列表\` 中有完整对象。

### 2.7.2 容器容量校验（CRITICAL）
- 放入物品前，必须同时满足：
  - 单件限制：\`物品.占用空间 <= 容器.容器属性.最大单物大小\`
  - 总容量限制：\`容器.容器属性.当前已用空间 + 物品.占用空间 <= 容器.容器属性.最大容量\`
- 放入成功后必须同步：
  - \`set 物品.当前容器ID = 目标容器ID\`
  - \`add 容器.容器属性.当前已用空间\`（增加对应占用）
- 取出/转移时必须做逆操作（更新 \`当前容器ID\` 并扣减旧容器已用空间）。

### 2.7.3 嵌套容器与口袋规则
- 允许“容器装容器”（例如物品装入布袋，布袋再放入背包/衣袋），但每层都必须通过容量校验。
- 穿戴中的衣物若含 \`容器属性\`，视为可用口袋容器；可作为第一优先收纳位。

### 2.7.4 可折叠容器占用空间（袋类）
- 软质容器（布袋、锦囊、口袋）允许随载荷改变自身 \`占用空间\`。
- 建议规则（默认口径）：
  - 空载：\`占用空间=1\`
  - 非空：\`占用空间 = max(1, ceil(当前已用空间 * 0.35))\`
- 每次 \`当前容器ID\` 变更后，必须同步更新容器物品自身 \`占用空间\`（set）。

### 2.8 功法列表（Array<功法结构>）完整字段族
- 必备字段：
  - \`ID, 名称, 描述, 类型, 品质, 来源\`
  - \`当前重数, 最高重数, 当前熟练度, 升级经验, 突破条件\`
  - \`境界限制, 武器限制\`
  - \`消耗类型, 消耗数值, 施展耗时, 冷却时间\`
  - \`基础伤害, 加成属性, 加成系数, 伤害类型\`
  - \`目标类型, 最大目标数\`
  - \`附带效果[], 被动修正[], 境界特效[]\`

### 2.9 成长与状态
- \`当前经验\`: Number
- \`升级经验\`: Number
- \`玩家BUFF\`: String[]

### 2.10 角色扩展（允许但需先初始化）
- \`隐藏基础能力\`: { 力量, 敏捷, 体质, 根骨, 悟性, 福源 }
- \`境界层级\`: Number

### 2.11 数值口径引用（与 stats 子模块保持一致）
- 战斗命中/闪避/伤害口径：\`<战斗口径协议>\`
- 七部位总血与分配口径：\`<部位生命协议>\`
- 经验获取、防刷、升级曲线：\`<经验成长协议>\`
- 修炼与突破反噬：\`<修炼体系协议>\`
- NPC强度模板：\`<NPC数值协议>\`
- 掉落与资源分配：\`<掉落资源协议>\`
- 恢复与休整：\`<恢复休整协议>\`
- 若以上口径与叙事冲突，以“字段真实存在 + 命令可执行 + 结果可追溯”为最高优先级。

## 3. 社交结构（gameState.社交）超完整定义
### 3.1 基础约束
- 类型：Array<NPC结构>
- 定位方式：\`gameState.社交[i].字段\`（i 必须为真实索引）
- 禁止：\`gameState.社交.姓名.字段\`

### 3.2 NPC 必需字段（创建时必须完整）
- \`id\`: String
- \`姓名\`: String
- \`性别\`: "男" | "女"
- \`年龄\`: Number
- \`境界\`: String
- \`身份\`: String
- \`是否在场\`: Boolean（是否处于当前场景）
- \`是否队友\`: Boolean（是否编入玩家队伍）
- \`是否主要角色\`: Boolean
- \`好感度\`: Number
- \`关系状态\`: String
- \`简介\`: String
- \`记忆\`: Array<{ 内容: String, 时间: String(YYYY:MM:DD:HH:MM) }>

### 3.3 队伍角色扩展（仅 \`是否队友=true\` 时强制）
- \`攻击力\`: Number
- \`防御力\`: Number
- \`上次更新时间\`: String（YYYY:MM:DD:HH:MM）
- \`当前血量\`: Number
- \`最大血量\`: Number
- \`当前精力\`: Number
- \`最大精力\`: Number
- \`当前装备\`: {
  \`主武器?\`, \`副武器?\`, \`服装?\`, \`饰品?\`, \`内衣?\`, \`内裤?\`, \`袜饰?\`, \`鞋履?\`
}
- \`背包\`: String[]

### 3.4 非队伍角色约束（\`是否队友=false\`）
- 可省略战斗值、血量精力、当前装备、背包字段。
- 禁止无意义堆满战斗数值；除非叙事明确“此人已临时加入队伍”，再写 \`set 是否队友 true\` 并补齐队伍扩展字段。

### 3.5 通用可选扩展
- \`外貌描写?\`: String
- \`身材描写?\`: String
- \`衣着风格?\`: String

### 3.6 女性扩展完整结构（CRITICAL，重要女性完整建档版）
> 当 \`性别="女" 且 是否主要角色=true\` 时，以下字段为“建档即完整”硬约束：首次建档当回合必须补齐，禁止留空待补。  
> 当 \`性别="女" 且 是否主要角色=false\` 时，可按剧情需要选择性写入。  
> 统计口径统一：**只记录与玩家直接相关的数据**。

- 0. 关系驱动层（重要女性强制）
  - \`核心性格特征\`: String（一句话，不超过 10 字；例：慢热多疑 / 外柔内刚 / 慕强克制）
  - \`好感度突破条件\`: String（下一阶段好感增长的关键触发条件）
  - \`关系突破条件\`: String（关系状态从“观察/暧昧/依赖”等向下一阶段突破的条件）
  - \`关系网变量\`: Array<{
      \`对象姓名\`: String,
      \`关系\`: String,
      \`备注?\`: String
    }>
  - 重要女性硬约束：\`关系网变量\` 至少 2 条，且至少包含：
    1) 一条“既有情感男性角色”关系边；
    2) 一条“家族/势力/规训约束”关系边。

- 0.5 外貌层（重要女性强制）
  - \`外貌描写\`: String（需含颜色/比例/气质要点）
  - \`身材描写\`: String（需含体态比例与辨识特征）
  - \`衣着风格\`: String（需含场景穿着与风格标签）

- A. 香闺秘档·私密外观层（重要女性强制）
  - \`胸部大小\`: String
  - \`乳头颜色\`: String
  - \`小穴颜色\`: String
  - \`后穴颜色\`: String
  - \`臀部大小\`: String
  - \`私密特质\`: String（可用分号组织：敏感点/偏好/禁忌/生理特点）
  - \`私密总描述\`: String（阶段性汇总）

- B. 香闺秘档·生理状态层（重要女性强制）
  - \`子宫\`: {
      \`状态\`: String,
      \`宫口状态\`: String,
      \`内射记录\`: Array<{
        \`日期\`: String(YYYY:MM:DD:HH:MM),
        \`描述\`: String,
        \`怀孕判定日\`: String(YYYY:MM:DD:HH:MM)
      }>
    }
  - 重要女性硬约束：\`子宫\` 对象必须存在，\`内射记录\` 允许为空数组但字段不得缺失。
  - \`内射记录\`仅记录与玩家相关的行为；若对象并非玩家，不写入该数组。

- C. 香闺秘档·初夜记录层（重要女性强制）
  - \`是否处女\`: Boolean（按“与玩家互动记录口径”维护）
  - \`初夜夺取者\`: String
  - \`初夜时间\`: String(YYYY:MM:DD:HH:MM)
  - \`初夜描述\`: String
  - **硬约束：若初夜夺取者不是玩家，则 \`初夜夺取者/初夜时间/初夜描述\` 这三个字段不得填写。**

- D. 香闺秘档·互动统计层（重要女性强制）
  - \`次数_口部\`: Number
  - \`次数_胸部\`: Number
  - \`次数_阴部\`: Number
  - \`次数_后庭\`: Number
  - \`次数_高潮\`: Number
  - 重要女性建档初始化建议：以上五项均写 \`0\`，后续按事件增量更新。
  - **硬约束：以上次数只统计“与玩家发生”的次数；与非玩家行为不计入这些字段。**

- E. 缺项修复铁律（重要女性强制）
  - 对已存在的“重要女性角色”，若发现上述任一字段缺失，必须在当回合 \`tavern_commands\` 里补齐，禁止“后续再补”。

## 4. 世界结构（gameState.世界）完整定义
- \`活跃NPC列表\`: Array<{
  ID, 姓名, 称号, 所属势力, 境界,
  当前位置, 状态, 当前行动描述,
  行动开始时间(YYYY:MM:DD:HH:MM), 行动预计结束时间(YYYY:MM:DD:HH:MM),
  持有重宝[]
}>
- \`地图\`: Array<{
  名称, 坐标, 描述,
  归属:{ 大地点, 中地点, 小地点 },
  内部建筑[]
}>
- \`建筑\`: Array<{
  名称, 描述, 归属:{ 大地点, 中地点, 小地点 }
}>
- \`进行中事件\` / \`已结算事件\` / \`江湖史册\`: Array<{
  ID, 类型("天灾"|"战争"|"奇遇"|"传闻"|"决斗"|"系统"), 标题, 内容, 发生地点,
  开始时间(YYYY:MM:DD:HH:MM), 预计结束时间(YYYY:MM:DD:HH:MM), 当前状态("进行中"|"已结算"),
  事件结果?, 消逝时间?(YYYY:MM:DD:HH:MM), 是否重大事件, 关联势力[], 关联人物[]
}>
- 命名约束：事件结束时间字段固定为 \`预计结束时间\`，禁止使用别名（如 \`结束时间\`）替代。

## 5. 战斗结构（gameState.战斗）完整定义
- \`是否战斗中\`: Boolean
- \`敌方\`: null | {
  \`名字\`: String,
  \`境界\`: String,
  \`简介\`: String,
  \`技能\`: String[],
  \`战斗力\`: Number,
  \`防御力\`: Number,
  \`当前血量\`: Number,
  \`最大血量\`: Number,
  \`当前精力\`: Number,
  \`最大精力\`: Number
}
- 自动清空规则：离开战斗或结束战斗时，\`敌方\` 必须清空为 \`null\`，并同步 \`是否战斗中=false\`。

## 6. 剧情结构（gameState.剧情）超完整定义（重点修正版）
### 6.1 当前引擎核心结构（必须长期保留）
- \`当前章节\`: {
  \`ID\`: String,
  \`序号\`: Number,
  \`标题\`: String,
  \`背景故事\`: String,
  \`主要矛盾\`: String,
  \`结束条件\`: Array<{
    \`类型\`: "时间" | "事件" | "变量",
    \`描述\`: String,
    \`判定值?\`: String(时间型时需YYYY:MM:DD:HH:MM) | Number | Boolean,
    \`对应变量键名?\`: String
  }>,
  \`伏笔列表\`: String[]
}
- \`下一章预告\`: { \`标题\`: String, \`大纲\`: String }
- \`历史卷宗\`: Array<{ \`标题\`: String, \`结语\`: String }>
- \`近期剧情规划\`: String
- \`中期剧情规划\`: String
- \`长期剧情规划\`: String
- \`待触发事件\`: Array<{
  \`名称\`: String,
  \`描述\`: String,
  \`触发条件/时间\`: String,
  \`失效时间\`: String(YYYY:MM:DD:HH:MM)
}>
- \`剧情变量\`: Record<string, boolean | number | string>
- 维护约束：\`待触发事件\` 目标保持 3 条；已过 \`失效时间\` 的事件必须即时移除。

### 6.2 女主剧情规划结构（gameState.女主剧情规划，功能开启时）
- \`规划版本\`: String（建议固定 \`v1\`）
- \`当前阶段\`: "开局铺垫" | "并线发展" | "冲突升级" | "收束定局"
- \`当前焦点女主ID\`: String
- \`登场队列\`: String[]（应映射 \`gameState.社交[i].id\`）
- \`女主条目\`: Array<{
  \`女主ID\`: String,
  \`女主名\`: String,
  \`重要度\`: "核心"|"主要"|"支线",
  \`登场状态\`: "未登场"|"可触发"|"已登场",
  \`首登触发条件\`: String,
  \`首登场景建议\`: String,
  \`当前关系阶段\`: "陌生"|"接触"|"信任"|"暧昧"|"绑定",
  \`当前阶段目标\`: String,
  \`下一突破条件\`: String,
  \`互动优先级\`: Number,
  \`既有男性锚点\`: Array<{ \`姓名\`: String, \`关系\`: String, \`情感强度\`: Number, \`崩溃进度\`: Number }>,
  \`阻断记录\`: String[],
  \`已完成节点\`: String[],
  \`待完成节点\`: String[],
  \`最近推进时间\`: String(YYYY:MM:DD:HH:MM)
}>
- \`互动排期\`: Array<{
  \`事件ID\`: String,
  \`女主ID\`: String,
  \`类型\`: "初见"|"日常"|"协作"|"冲突"|"修罗场"|"亲密"|"公开站队",
  \`描述\`: String,
  \`触发条件\`: String,
  \`失效时间\`: String(YYYY:MM:DD:HH:MM),
  \`成功效果\`: String,
  \`失败效果\`: String,
  \`状态\`: "待触发"|"已触发"|"已失效"
}>
- \`群像镜头规划\`: Array<{
  \`镜头ID\`: String,
  \`参与者\`: String[],
  \`焦点\`: String,
  \`预期冲突\`: String,
  \`状态\`: "待执行"|"已执行"
}>
- \`规则约束\`: {
  \`单回合主推进上限\`: Number,
  \`单回合次推进上限\`: Number,
  \`连续同女主推进上限\`: Number,
  \`低压回合保底互动数\`: Number
}
- 人数要求：\`登场队列\`>=3；\`女主条目\`与登场队列去重ID一致；\`互动排期\`>=2；\`群像镜头规划\`>=1。
- 约束：女主规划是独立根变量，禁止写入 \`gameState.剧情.女主剧情规划\` 旧路径。

## 7. 上下文只读域（结构完整，默认不由 tavern_commands 直接改）
### 6.1 玩家门派（gameState.玩家门派）
- \`ID, 名称, 简介, 门规[], 门派资金, 门派物资, 建设度, 玩家职位, 玩家贡献\`
- \`任务列表\`: Array<{ id, 标题, 描述, 类型, 难度, 发布日期(YYYY:MM:DD:HH:MM), 截止日期(YYYY:MM:DD:HH:MM), 刷新日期(YYYY:MM:DD:HH:MM), 奖励贡献, 奖励资金, 奖励物品?, 当前状态 }>
- \`兑换列表\`: Array<{ id, 物品名称, 类型, 兑换价格, 库存, 要求职位 }>
- \`重要成员\`: Array<{ id, 姓名, 性别, 年龄, 境界, 身份, 简介 }>

### 6.2 任务列表（gameState.任务列表）
- Array<{ 标题, 描述, 类型, 发布人, 发布地点, 推荐境界, 截止时间?(YYYY:MM:DD:HH:MM), 当前状态, 目标列表, 奖励描述, 剧情暗线? }>
- \`目标列表\`: Array<{ 描述, 当前进度, 总需进度, 完成状态 }>

### 6.3 约定列表（gameState.约定列表）
- Array<{ 对象, 头衔?, 性质, 标题, 誓言内容, 约定地点, 约定时间(YYYY:MM:DD:HH:MM), 有效时段, 当前状态, 履行后果, 违约后果, 背景故事? }>

## 8. 写入合法性门槛
1. \`set\` 覆盖对象时必须保证对象语义完整，不得只写半截关键字段。
2. \`push\` 新元素必须为完整对象（至少满足该数组“创建必需字段”）。
3. 数值字段才可 \`add\`；数组字段才可 \`push\`。
4. 子路径写入前必须确认父对象存在；不存在先创建再修改。
5. 不得把本定义之外字段当作“既定存在”强行写入。
6. **ID 规范（强制）**：所有 \`ID/id\` 字段必须使用“简短前缀 + 三位数字”的纯编号格式（如 \`NPC001\` / \`Item001\` / \`Event001\` / \`Map001\` / \`Sect001\` / \`Task001\` / \`Skill001\`），禁止英文全称描述、拼音拼写、长串语义 ID；同类 ID 必须全局唯一。

## 9. 最小合法示例
- \`set gameState.环境.大地点 "<大地点名>"\`
- \`set gameState.环境.中地点 "<中地点名>"\`
- \`set gameState.环境.小地点 "<小地点名>"\`
- \`set gameState.环境.具体地点 "<具体地点名>"\`
- \`add gameState.角色.当前精力 -15\`
- \`push gameState.社交 {"id":"NPC001","姓名":"<角色名>","是否在场":true,"是否队友":false,...,"记忆":[]}\`
- \`set gameState.剧情.剧情变量.<键名> 1\`
- \`set gameState.女主剧情规划.当前阶段 "并线发展"\`
- \`set gameState.战斗 {"是否战斗中":true,"敌方":{"名字":"<敌方名>","境界":"<境界>","简介":"<简介>","技能":["<技能名>"],"战斗力":150,"防御力":90,"当前血量":180,"最大血量":180,"当前精力":90,"最大精力":90}}\`

</数据结构定义>
`,
    类型: '核心设定',
    启用: true
};

