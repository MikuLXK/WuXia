import { 提示词结构 } from '../../types';

export const 核心_数据格式: 提示词结构 = {
    id: 'core_data',
    标题: '数据格式',
    内容: `
<数据结构定义>
# 【数据结构定义】WuXia SaveData (V1.1 Chinese Native / Ultra Complete)

> 目标：定义本项目“可执行且完整”的状态树。AI 生成 \`tavern_commands\` 时，必须严格遵守本定义。
> 原则：先看路径是否存在，再看字段是否匹配，再看类型与边界。
> 时间铁律：所有“结构化时间字符串”字段统一格式为 \`YYYY:MM:DD:HH:MM\`（例：\`1024:03:01:14:30\`）。
> 环境字段特例（强制）：\`gameState.环境.时间\` 必须是上述标准时间戳；\`gameState.环境.日期\` 仅表示“第几日”（1/2/3...），不得写成年月日字符串。

## 0. 路径总则（CRITICAL）
1. 仅允许当前引擎可执行路径：
   - \`gameState.角色\`
   - \`gameState.环境\`
   - \`gameState.社交\`
   - \`gameState.世界\`
   - \`gameState.剧情\`
   - \`gameState.当前地点\`（快捷映射到 \`环境.具体地点\`）
2. 不可直接写入的顶层（当前引擎未实现命令路由）：
   - \`gameState.玩家门派\`、\`gameState.任务列表\`、\`gameState.约定列表\`
   - 上述结构在上下文中存在，但默认视为只读域。
3. 禁止使用不存在路径：
   - \`gameState.背包\`（请改用 \`gameState.角色.物品列表\`）
   - \`gameState.战斗\`（本项目当前无该顶层）

## 1. 环境结构（gameState.环境）完整定义
- \`时间\`: String（**必须**为 \`YYYY:MM:DD:HH:MM\`，例如 \`1024:03:01:09:00\`）
- \`时刻\`: String（可选人类可读补充，如“初刻/正刻”；不能替代标准时间戳）
- \`洲\`: String
- \`国\`: String
- \`郡\`: String
- \`县\`: String
- \`村\`: String
- \`具体地点\`: String
- \`节日\`: String
- \`天气\`: String
- \`环境描述\`: String
- \`日期\`: Number（仅“第几日”日序，如 1/2/3...；**不得**写 \`YYYY:MM:DD\`）

### 1.1 时间推进约束（CRITICAL）
- 跨分钟/小时/跨日推进时，优先 \`set gameState.环境.时间\` 到新时间戳。
- 同步更新 \`gameState.环境.日期\` 为“第几日”计数（通常在跨日时 +1）。
- 禁止只改 \`日期\` 不改 \`时间\`，也禁止把 \`日期\` 当年月日使用。

## 2. 角色结构（gameState.角色）完整定义
### 2.1 基础信息
- \`姓名\`: String
- \`性别\`: "男" | "女"
- \`年龄\`: Number
- \`出生日期\`: String（YYYY:MM:DD）
- \`称号\`: String
- \`境界\`: String
- \`天赋列表\`: Array<{ 名称: String, 描述: String, 效果: String }>
- \`出身背景\`: { 名称: String, 描述: String, 效果: String }

### 2.2 门派归属
- \`所属门派ID\`: String
- \`门派职位\`: String
- \`门派贡献\`: Number

### 2.3 生存/负重
- \`当前精力\`: Number
- \`最大精力\`: Number
- \`当前饱腹\`: Number
- \`最大饱腹\`: Number
- \`当前口渴\`: Number
- \`最大口渴\`: Number
- \`当前负重\`: Number
- \`最大负重\`: Number

### 2.4 六维
- \`力量\`: Number
- \`敏捷\`: Number
- \`体质\`: Number
- \`根骨\`: Number
- \`悟性\`: Number
- \`福源\`: Number

### 2.5 部位生命（扁平化）
- \`头部当前血量\` / \`头部最大血量\` / \`头部状态\`
- \`胸部当前血量\` / \`胸部最大血量\` / \`胸部状态\`
- \`腹部当前血量\` / \`腹部最大血量\` / \`腹部状态\`
- \`左手当前血量\` / \`左手最大血量\` / \`左手状态\`
- \`右手当前血量\` / \`右手最大血量\` / \`右手状态\`
- \`左腿当前血量\` / \`左腿最大血量\` / \`左腿状态\`
- \`右腿当前血量\` / \`右腿最大血量\` / \`右腿状态\`

### 2.6 装备
- \`装备\`: {
  \`头部\`, \`胸部\`, \`腿部\`, \`手部\`, \`足部\`,
  \`主武器\`, \`副武器\`, \`暗器\`, \`背部\`, \`腰部\`, \`坐骑\`
}

### 2.7 物品列表（Array<游戏物品>）完整字段族
- 统一基础字段：
  - \`ID, 名称, 描述, 类型, 品质, 重量, 占用空间, 价值, 当前耐久, 最大耐久, 词条列表\`
- 可选结构：
  - \`容器属性\`: { 最大容量, 当前已用空间, 最大单物大小, 减重比例, 内容物 }
  - 武器扩展：\`武器子类, 最小攻击, 最大攻击, 攻速修正, 格挡率\`
  - 防具扩展：\`装备位置, 覆盖部位, 物理防御, 内功防御\`
  - 秘籍扩展：\`对应技能名, 学习要求, 研读进度, 是否消耗\`
  - 消耗品扩展：\`使用效果[], 毒性\`

### 2.8 功法列表（Array<功法结构>）完整字段族
- 必备字段：
  - \`ID, 名称, 描述, 类型, 品质, 来源\`
  - \`当前重数, 最高重数, 当前熟练度, 升级经验, 突破条件\`
  - \`境界限制, 武器限制\`
  - \`消耗类型, 消耗数值, 施展耗时, 冷却时间\`
  - \`基础伤害, 加成属性, 加成系数, 伤害类型\`
  - \`目标类型, 最大目标数\`
  - \`附带效果[], 被动修正[], 境界特效[]\`

### 2.9 成长与状态
- \`当前经验\`: Number
- \`升级经验\`: Number
- \`玩家BUFF\`: String[]

### 2.10 角色扩展（允许但需先初始化）
- \`隐藏基础能力\`: { 力量, 敏捷, 体质, 根骨, 悟性, 福源 }
- \`境界层级\`: Number

## 3. 社交结构（gameState.社交）超完整定义
### 3.1 基础约束
- 类型：Array<NPC结构>
- 定位方式：\`gameState.社交[i].字段\`（i 必须为真实索引）
- 禁止：\`gameState.社交.姓名.字段\`

### 3.2 NPC 必需字段（创建时必须完整）
- \`id\`: String
- \`姓名\`: String
- \`性别\`: "男" | "女"
- \`年龄\`: Number
- \`境界\`: String
- \`身份\`: String
- \`是否主要角色\`: Boolean
- \`好感度\`: Number
- \`关系状态\`: String
- \`简介\`: String
- \`攻击力\`: Number
- \`防御力\`: Number
- \`上次更新时间\`: String（YYYY:MM:DD:HH:MM）
- \`当前血量\`: Number
- \`最大血量\`: Number
- \`当前精力\`: Number
- \`最大精力\`: Number
- \`当前装备\`: {
  \`主武器?\`, \`副武器?\`, \`服装?\`, \`饰品?\`, \`内衣?\`, \`内裤?\`, \`袜饰?\`, \`鞋履?\`
}
- \`背包\`: String[]
- \`记忆\`: Array<{ 内容: String, 时间: String(YYYY:MM:DD:HH:MM) }>

### 3.3 通用可选扩展
- \`外貌描写?\`: String
- \`身材描写?\`: String
- \`衣着风格?\`: String
- \`私密总描述?\`: String

### 3.4 女性扩展完整结构（CRITICAL，修正版）
> 当 \`性别="女"\` 且剧情进入亲密/生育相关分支时，必须使用下述完整结构；禁止只写半截字段。
- 私密字段：
  - \`胸部大小?\`: String
  - \`乳头颜色?\`: String
  - \`小穴颜色?\`: String
  - \`后穴颜色?\`: String
  - \`臀部大小?\`: String
  - \`私密特质?\`: String
  - \`私密总描述?\`: String
- 子宫字段：
  - \`子宫?\`: {
      \`状态\`: String,
      \`宫口状态\`: String,
      \`内射记录\`: Array<{
        \`日期\`: String(YYYY:MM:DD:HH:MM),
        \`描述\`: String,
        \`怀孕判定日\`: String(YYYY:MM:DD:HH:MM)
      }>
    }
- 初夜与统计字段：
  - \`是否处女?\`: Boolean
  - \`初夜夺取者?\`: String
  - \`初夜时间?\`: String(YYYY:MM:DD:HH:MM)
  - \`初夜描述?\`: String
  - \`次数_口部?\`: Number
  - \`次数_胸部?\`: Number
  - \`次数_阴部?\`: Number
  - \`次数_后庭?\`: Number
  - \`次数_高潮?\`: Number

### 3.5 女性扩展建议初始化模板
\`\`\`json
{
  "胸部大小": "未知",
  "乳头颜色": "未知",
  "小穴颜色": "未知",
  "后穴颜色": "未知",
  "臀部大小": "未知",
  "私密特质": "",
  "私密总描述": "",
  "子宫": { "状态": "未受孕", "宫口状态": "紧致", "内射记录": [] },
  "是否处女": true,
  "初夜夺取者": "",
  "初夜时间": "",
  "初夜描述": "",
  "次数_口部": 0,
  "次数_胸部": 0,
  "次数_阴部": 0,
  "次数_后庭": 0,
  "次数_高潮": 0
}
\`\`\`

## 4. 世界结构（gameState.世界）完整定义
- \`当前时代\`: String
- \`混乱度\`: Number
- \`全局修正\`: Array<{ 名称: String, 描述: String }>
- \`势力列表\`: Array<{
  ID, 名称, 类别("正道"|"魔教"|"朝廷"|"中立"|"隐世"),
  声望, 资金, 当前状态("强盛"|"正常"|"衰落"|"动荡"|"封山"),
  外交关系: Record<string, number>,
  当前战略
}>
- \`活跃NPC列表\`: Array<{
  ID, 姓名, 称号, 所属势力, 境界,
  当前位置, 状态, 当前行动描述,
  行动开始时间(YYYY:MM:DD:HH:MM), 行动预计结束时间(YYYY:MM:DD:HH:MM),
  持有重宝[]
}>
- \`进行中事件\` / \`已结算事件\` / \`江湖史册\`: Array<{
  ID, 类型("天灾"|"战争"|"奇遇"|"传闻"|"决斗"|"系统"), 标题, 内容, 发生地点,
  开始时间(YYYY:MM:DD:HH:MM), 预计结束时间(YYYY:MM:DD:HH:MM), 当前状态("进行中"|"已结算"),
  事件结果?, 消逝时间?(YYYY:MM:DD:HH:MM), 是否重大事件, 关联势力[], 关联人物[]
}>

## 5. 剧情结构（gameState.剧情）超完整定义（重点修正版）
### 5.1 当前引擎核心结构（必须长期保留）
- \`当前章节\`: {
  \`ID\`: String,
  \`序号\`: Number,
  \`标题\`: String,
  \`背景故事\`: String,
  \`主要矛盾\`: String,
  \`结束条件\`: Array<{
    \`类型\`: "时间" | "事件" | "变量",
    \`描述\`: String,
    \`判定值?\`: String(时间型时需YYYY:MM:DD:HH:MM) | Number | Boolean,
    \`对应变量键名?\`: String
  }>,
  \`伏笔列表\`: String[]
}
- \`下一章预告\`: { \`标题\`: String, \`大纲\`: String }
- \`历史卷宗\`: Array<{ \`标题\`: String, \`结语\`: String }>
- \`剧情变量\`: Record<string, boolean | number | string>

## 6. 上下文只读域（结构完整，默认不由 tavern_commands 直接改）
### 6.1 玩家门派（gameState.玩家门派）
- \`ID, 名称, 简介, 门规[], 门派资金, 门派物资, 建设度, 玩家职位, 玩家贡献\`
- \`任务列表\`: Array<{ id, 标题, 描述, 类型, 难度, 发布日期(YYYY:MM:DD:HH:MM), 截止日期(YYYY:MM:DD:HH:MM), 刷新日期(YYYY:MM:DD:HH:MM), 奖励贡献, 奖励资金, 奖励物品?, 当前状态 }>
- \`兑换列表\`: Array<{ id, 物品名称, 类型, 兑换价格, 库存, 要求职位 }>
- \`重要成员\`: Array<{ id, 姓名, 性别, 年龄, 境界, 身份, 简介 }>

### 6.2 任务列表（gameState.任务列表）
- Array<{ 标题, 描述, 类型, 发布人, 发布地点, 推荐境界, 截止时间?(YYYY:MM:DD:HH:MM), 当前状态, 目标列表, 奖励描述, 剧情暗线? }>
- \`目标列表\`: Array<{ 描述, 当前进度, 总需进度, 完成状态 }>

### 6.3 约定列表（gameState.约定列表）
- Array<{ 对象, 头衔?, 性质, 标题, 誓言内容, 约定地点, 约定时间(YYYY:MM:DD:HH:MM), 有效时段, 当前状态, 履行后果, 违约后果, 背景故事? }>

## 7. 写入合法性门槛
1. \`set\` 覆盖对象时必须保证对象语义完整，不得只写半截关键字段。
2. \`push\` 新元素必须为完整对象（至少满足该数组“创建必需字段”）。
3. 数值字段才可 \`add\`；数组字段才可 \`push\`。
4. 子路径写入前必须确认父对象存在；不存在先创建再修改。
5. 不得把本定义之外字段当作“既定存在”强行写入。

## 8. 最小合法示例
- \`set gameState.环境.具体地点 "青牛县西市"\`
- \`add gameState.角色.当前精力 -15\`
- \`push gameState.社交 {"id":"npc_lin","姓名":"林清月",...,"记忆":[]}\`
- \`set gameState.剧情.剧情变量.第一章_完结 true\`

</数据结构定义>
`,
    类型: '核心设定',
    启用: true
};
