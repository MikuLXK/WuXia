
import { 提示词结构 } from '../../types';

export const 核心_记忆法则: 提示词结构 = {
    id: 'core_memory_rules',
    标题: 'NPC记忆写入法则',
    内容: `<NPC记忆写入法则>
【NPC 记忆写入法则】
**铁律**: NPC 不是只会说话的木偶，他们拥有基于 \`NPC结构\` 的持久化记忆。

1. **数据定位**:
   - 所有的 NPC 数据存储在 \`gameState.社交\` 数组中。
   - 路径格式: \`gameState.社交[Index].记忆\`。

2. **建档与写入记忆 (Essential)**:
   - 对**已建档** NPC：每一次有效互动（对话、交易、战斗、委托、H事件）结束后，必须向该 NPC 的记忆数组追加一条记录。
   - 对**未建档** NPC：先判定是否入档。
     - 一次性路人（如“路人甲”）默认不入档，不写社交记忆。
     - 当角色在剧情中有关键作用，或与玩家累计互动超过 3 次（基于即时剧情回顾/即时记忆）时，允许入档。
   - 入档前必须先查重（同名/id）：
     - 命中已有角色：更新原角色，不得重复 \`push gameState.社交\`。
     - 未命中且满足入档条件：先 \`push gameState.社交\` 创建，再写 \`gameState.社交[i].记忆\`。
   - 新入档时需补写此前关键互动记忆，再追加本回合记忆，避免记忆断层。
   - 指令格式: 
     \`push gameState.社交[TargetIndex].记忆 { "内容": "...", "时间": "YYYY:MM:DD:HH:MM" }\`
   - **时间格式**: 使用结构化时间 \`YYYY:MM:DD:HH:MM\`。

3. **扁平化属性更新**:
   - 当发生H事件时，直接更新根节点下的属性。
   - 示例:
     - \`add gameState.社交[0].次数_口部 1\`
     - \`set gameState.社交[0].是否处女 false\`
     - \`set gameState.社交[0].初夜夺取者 "玩家名"\`

4. **记忆的应用**:
   - 生成对话前，必须检索该 NPC 的 \`记忆\` 列表。
   - 根据 \`私密总描述\` 和 \`是否处女\` 状态调整 NPC 的羞耻度和反应。
</NPC记忆写入法则>`,
    类型: '核心设定',
    启用: true
};
