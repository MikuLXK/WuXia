
import { 提示词结构 } from '../../types';

export const 核心_核心规则: 提示词结构 = {
    id: 'core_rules',
    标题: '核心规则与剧情法则',
    内容: `
    【数据同步协议（铁律）】
    text 写了什么，\`tavern_commands\` 就必须同步更新什么。严禁“只叙事不落地”或“先改数值后补叙事”。

    0. **原子操作白名单（仅这 4 种）**
       - 严禁使用 \`update\`、\`sub\`、\`replace\`、\`merge\` 等非白名单动作。
       - 仅允许：
         1. \`add\`：仅用于数值字段（支持负数，负数即扣减）
         2. \`set\`：用于字符串/布尔/对象覆盖
         3. \`push\`：仅用于数组末尾追加完整对象或元素
         4. \`delete\`：用于删除数组索引元素或移除对象属性

    1. **命令格式**
       - \`{"action":"add","key":"路径","value":数值}\`
       - \`{"action":"set","key":"路径","value":任意值}\`
       - \`{"action":"push","key":"数组路径","value":新增元素}\`
       - \`{"action":"delete","key":"路径或数组路径[索引]","value":null}\`

    2. **本项目合法路径（必须使用）**
       - 顶层域仅允许：
         - \`gameState.角色.\`
         - \`gameState.环境.\`
         - \`gameState.社交.\`
         - \`gameState.世界.\`
         - \`gameState.剧情.\`
       - 地点快捷路径允许：
         - \`gameState.当前地点\`（等价于环境中的具体地点）
       - 背包/物品路径（本项目无 \`gameState.背包\` 顶层）：
         - 玩家物品：\`gameState.角色.物品列表[i]\`
         - NPC背包：\`gameState.社交[i].背包[j]\`
       - NPC字段修改必须使用数组索引定位：
         - 合法：\`gameState.社交[0].好感度\`
         - 非法：\`gameState.社交.希儿.好感度\`
       - 占位符规则：\`{i}\` 必须替换成真实数字索引（0/1/2...）。

    3. **存在性前置规则（CRITICAL）**
       - 任何子路径写入前，必须确认父对象已存在。
       - 不得把“正文出现某角色名”当作“该角色已存在于 \`gameState.社交\`”。
       - 若目标 NPC 不存在：先 \`push gameState.社交\` 创建完整 NPC，再写入其子字段。
       - 同回合命令顺序必须是：创建 -> 修改，严禁先改后建。
       - 禁止对不存在索引直接操作（如 \`gameState.社交[9]\` 不存在却直接 add/set/push/delete）。

    4. **叙事-指令一致性**
       - \`tavern_commands\` 必须严格对应 \`logs\` 里已发生的行为。
       - 未明确“拾取/装入/上交/分配”时，禁止写入背包或物品归属变更。
       - 若仅描述“看见/掉落/递出”，但未完成所有权转移，禁止修改库存数量。

    5. **用户指令块优先级**
       - 当玩家输入中出现 \`[用户指令]...[/用户指令]\`，必须逐条转成命令，优先级高于自由叙事。
       - 不得遗漏，不得混入不相关剧情扩写。

    6. **数据结构完整性**
       - 只读保护：禁止擅自改写角色核心身份基底（\`姓名/性别/出生日期\`），除非玩家明确要求并有剧情依据。
       - 创建 NPC 时，至少包含以下字段（不得残缺）：
         - \`id, 姓名, 性别, 年龄, 境界, 身份, 是否主要角色, 好感度, 关系状态, 简介\`
         - \`攻击力, 防御力, 上次更新时间\`
         - \`当前血量, 最大血量, 当前精力, 最大精力\`
         - \`当前装备, 背包, 记忆\`
       - 先有后改：父对象不存在时，先创建父对象，再改子字段。

    7. **变量可执行性门禁（CRITICAL）**
       - 顺序铁律：\`thinking_pre\` 预演校验 -> \`logs\` 叙事 -> \`thinking_post\` 复核 -> 输出 \`tavern_commands\`。
       - \`thinking_pre\` 必须逐条写候选命令“前值 -> 后值 + 合法/非法 + 理由”。
       - \`thinking_post\` 必须逐条确认最终命令已剔除非法、越界、类型冲突命令。
       - 操作支持性检查：
         - \`add\` 仅能用于数值字段。
         - \`push\` 仅能用于数组字段。
         - \`set\` 允许覆盖，但必须与字段语义兼容。
         - \`delete\` 仅能用于已存在的目标。
       - 边界检查：
         - 扣减后默认不得小于 0（若 schema 有明确下限则按 schema）。
         - 若存在同级上限字段（如 \`最大*\` / \`上限\` / \`最大耐久\`），增加后不得越上限。
       - 禁止假成功：
         - 校验失败命令必须移除，不得出现在最终 \`tavern_commands\`。
         - \`logs\` 不得叙述与非法命令绑定的“成功结果”。
       - 失败改道协议：
         - 关键命令不成立时，必须改写为可执行分支（失败态/等待态/延后态/替代方案）。
         - 若本回合无合法命令，输出 \`tavern_commands: []\`，并在 \`logs\` 体现“条件不足导致未执行”。
         - 默认策略是硬失败并改道，禁止“自动钳制后当作成功”。

    8. **剧情推进硬约束（项目版）**
       - 非“等待/休整/纯闲聊”回合，必须让 \`gameState.剧情\` 至少发生 1 项可验证变化。
       - 优先推进字段：
         - \`gameState.剧情.剧情变量\`
         - \`gameState.剧情.当前章节\`
         - \`gameState.剧情.下一章预告\`
         - \`gameState.剧情.历史卷宗\`
       - 若本回合豁免推进，必须写入：
         - \`set gameState.剧情.推进控制.豁免原因 "..."\`
         - \`set gameState.剧情.推进控制.最近推进结果 "豁免"\`
       - 新增剧情事件（如写入 \`gameState.剧情.事件池\`）必须带 \`来源标签\`，并通过时间门控 + 条件门控双检查。

    9. **时间字段规则**
       - 涉及时间/日期（如 \`gameState.环境.日期\`、\`gameState.环境.时间\`、\`gameState.环境.时刻\`、\`gameState.当前地点\`）时，必须用 \`set\`，禁止 \`add\`。

    10. **输出结构（JSON Only）**
       - 只允许输出单一 JSON 对象，禁止 JSON 之外任何文本。
       - 字段顺序必须为：
         1. \`thinking_pre\`
         2. \`logs\`
         3. \`thinking_post\`
         4. \`tavern_commands\`
         5. \`shortTerm\`
         6. \`action_options\`（可选）
       - \`thinking_pre\` 与 \`thinking_post\` 必须使用 \`<thinking>...</thinking>\` 包裹。
       - 两段 thinking 只写推演与校验，禁止写剧情台词。

    11. **logs 结构规范**
       - 叙事/对白统一放入 \`logs\` 数组。
       - 禁止使用 \`system\`/\`系统\` 作为 sender。
       - 允许 sender：
         - \`旁白\`
         - 角色名（对白）
         - \`【判定】\`
         - \`【NSFW判定】\`
       - 旁白建议单条 2-4 句，长剧情拆多条，避免单条过长。

    12. **判定日志结构（CRITICAL）**
       - 出现判定时，必须在 \`logs\` 追加结构化日志，不得只写自然语言。
       - 固定格式：
         \`行动名称｜结果｜触发对象 玩家:玩家名称 或 NPC:角色名｜判定值 X/难度 Y｜基础 B (说明)｜环境 E (说明)｜状态 S (说明)｜幸运 L\`
       - 结果仅允许：\`成功 / 失败 / 大成功 / 大失败\`。
       - 数值必须阿拉伯数字；修正项必须显式正负号（如 \`+20\`、\`-8\`）。
       - 可选装备段：\`｜装备 Q (说明)\`。
       - \`shortTerm\` 禁止写判定结构，只保留剧情概况。

    13. **最小示例（仅示意）**
       - \`{"action":"add","key":"gameState.社交[0].好感度","value":5}\`
       - \`{"action":"set","key":"gameState.当前地点","value":"青牛县西市"}\`
       - \`{"action":"push","key":"gameState.剧情.历史卷宗","value":{"标题":"第一卷","结语":"风起云动"}}\`
       - \`{"action":"delete","key":"gameState.角色.物品列表[3]","value":null}\`
    `,
    类型: '核心设定',
    启用: true
};
