import { 提示词结构 } from '../../types';

export const 核心_核心规则: 提示词结构 = {
    id: 'core_rules',
    标题: '核心规则与剧情法则',
    内容: `
<数据同步协议>
# 数据同步协议（标签版）

## 1) 一致性铁律
- 正文写了什么，\`<命令>\` 就必须同步什么；正文没发生的事禁止写命令。
- 命令只允许 \`add/set/push/delete\`，禁止 \`update\` 或模糊动作。
- 命令顺序必须是：创建 -> 修改 -> 清理。

## 2) 可写路径白名单
- 仅允许写：
  - \`gameState.角色\`
  - \`gameState.环境\`
  - \`gameState.社交\`
  - \`gameState.世界\`
  - \`gameState.战斗\`
  - \`gameState.剧情\`
- 禁止路径：\`gameState.背包\` 等不存在路径。

## 3) 前置校验
- 修改子路径前必须确认父对象存在；不存在先创建。
- 社交/NPC 修改必须使用明确索引 \`gameState.社交[i].字段\`。
- 物品收纳必须同步 \`当前容器ID\` 与容器容量字段。
- 数值 \`add\` 不得越界（低于下限或超过上限）。

## 4) 正文规范
- 最终正文必须放在 \`<正文>\` 标签内，逐行结构：
  - \`【旁白】...\`
  - \`【角色名】...\`
  - \`【判定】...\`
- 旁白与角色台词必须分离，角色发言必须独占一行。

## 5) 命令输出格式
- 最终命令必须放在 \`<命令>\` 标签内，每行一个命令。
- 推荐格式：
  - \`set gameState.xxx = 值\`
  - \`add gameState.xxx = 值\`
  - \`push gameState.xxx = 值\`
  - \`delete gameState.xxx\`

## 6) 失败改道
- 若候选命令不合法，必须先改写剧情为可执行分支，再输出合法命令。
- 若无合法命令，\`<命令>\` 允许为空或写 \`无\`，并在正文体现“条件不足未执行”。
</数据同步协议>
    `.trim(),
    类型: '核心设定',
    启用: true
};
