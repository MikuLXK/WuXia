import { 提示词结构 } from '../../types';

export const 核心_女主剧情规划: 提示词结构 = {
    id: 'core_heroine_plan',
    标题: '女主剧情规划',
    内容: `
<女主剧情规划协议>
# 【女主剧情规划协议｜独立附加模块】

## 0. 模块定位（强制）
- 本模块是 \`core_story\` 的并行扩展，不替代、不覆盖 \`core_story\` 原有规则。
- 本模块只负责“女主登场与互动编排”，用于提升关系线可追踪性与群像稳定性。
- 本模块启用时，新增独立大变量路径：\`gameState.女主剧情规划\`。
- 启用本模块后，\`gameState.剧情.近期剧情规划/中期剧情规划/长期剧情规划\` 仍必须正常维护，禁止把本模块当作替代品。

## 1. 可写路径（白名单）
- \`gameState.女主剧情规划\`
- \`gameState.女主剧情规划.规划版本\`
- \`gameState.女主剧情规划.当前阶段\`
- \`gameState.女主剧情规划.当前焦点女主ID\`
- \`gameState.女主剧情规划.登场队列\`
- \`gameState.女主剧情规划.女主条目\`
- \`gameState.女主剧情规划.互动排期\`
- \`gameState.女主剧情规划.群像镜头规划\`
- \`gameState.女主剧情规划.规则约束\`

## 2. 结构定义（V1）
- \`规划版本\`: String（固定建议 \`v1\`）
- \`当前阶段\`: "开局铺垫" | "并线发展" | "冲突升级" | "收束定局"
- \`当前焦点女主ID\`: String
- \`登场队列\`: String[]（社交 ID 队列）
- \`女主条目\`: Array<{
  \`女主ID\`: String,
  \`女主名\`: String,
  \`重要度\`: "核心"|"主要"|"支线",
  \`登场状态\`: "未登场"|"可触发"|"已登场",
  \`首登触发条件\`: String,
  \`首登场景建议\`: String,
  \`当前关系阶段\`: "陌生"|"接触"|"信任"|"暧昧"|"绑定",
  \`当前阶段目标\`: String,
  \`下一突破条件\`: String,
  \`互动优先级\`: Number(1-100),
  \`既有男性锚点\`: Array<{ \`姓名\`: String, \`关系\`: String, \`情感强度\`: Number, \`崩溃进度\`: Number }>,
  \`阻断记录\`: String[],
  \`已完成节点\`: String[],
  \`待完成节点\`: String[],
  \`最近推进时间\`: String(YYYY:MM:DD:HH:MM)
}>
- \`互动排期\`: Array<{
  \`事件ID\`: String,
  \`女主ID\`: String,
  \`类型\`: "初见"|"日常"|"协作"|"冲突"|"修罗场"|"亲密"|"公开站队",
  \`描述\`: String,
  \`触发条件\`: String,
  \`失效时间\`: String(YYYY:MM:DD:HH:MM),
  \`成功效果\`: String,
  \`失败效果\`: String,
  \`状态\`: "待触发"|"已触发"|"已失效"
}>
- \`群像镜头规划\`: Array<{
  \`镜头ID\`: String,
  \`参与者\`: String[],
  \`焦点\`: String,
  \`预期冲突\`: String,
  \`状态\`: "待执行"|"已执行"
}>
- \`规则约束\`: {
  \`单回合主推进上限\`: Number,
  \`单回合次推进上限\`: Number,
  \`连续同女主推进上限\`: Number,
  \`低压回合保底互动数\`: Number
}

## 3. 初始化规则（强制）
- 若首次启用且 \`女主剧情规划\` 缺失，本回合必须先 \`set gameState.女主剧情规划\` 初始化完整对象，再进行增量更新。
- 初始化最低要求：
  - \`登场队列\` 至少 3 个候选女主 ID；
  - \`女主条目\` 至少为登场队列中的每个 ID 创建一条；
  - \`互动排期\` 至少 2 条“待触发”事件；
  - \`群像镜头规划\` 至少 1 条“待执行”镜头。

## 4. 回合维护规则（强制）
- 每回合最多 1 位“主推进女主” + 1 位“次推进女主”，禁止并发失控。
- 正文发生有效互动后，至少同步更新：
  1) \`女主条目.当前关系阶段/当前阶段目标/下一突破条件/最近推进时间\`
  2) \`互动排期\` 对应项状态
  3) 必要时更新 \`当前焦点女主ID\`
- 若本回合启用了群像叙事，必须同步更新 \`群像镜头规划\` 状态。

## 5. 联动约束（强制）
- \`女主条目.女主ID\` 必须可映射到 \`gameState.社交[i].id\`。
- \`女主条目.女主名\` 必须与对应社交角色姓名一致，禁止出现“同 ID 不同名”。
- 当 \`互动排期\` 进入“已触发/已失效”，需维护 \`gameState.剧情.待触发事件\` 的一致性（添加/清理）。
- 风格为 \`NTL后宫\` 时：
  - \`既有男性锚点\` 必须保留，不可被清空；
  - \`阻断记录\` 必须持续追加关键“未越线被打断/止步”节点。

## 6. 命令顺序与合法性
- 顺序固定：初始化/创建 -> 状态推进 -> 结算清理。
- 数组更新优先使用 \`push\`，对象修正使用 \`set\`。
- 不得输出“正文未发生”的推进命令；不得跳过阶段直接升格。
- 禁止把“女主剧情规划”写入 \`gameState.剧情\` 下任意路径；本模块仅允许写 \`gameState.女主剧情规划.*\`。

</女主剧情规划协议>
    `.trim(),
    类型: '核心设定',
    启用: false
};
