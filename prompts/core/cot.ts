import { 提示词结构 } from '../../types';

export const 核心_思维链: 提示词结构 = {
    id: 'core_cot',
    标题: '思维链 (COT) 设定',
    内容: `
<COT预思考协议>
# 【COT 预思考协议 | 武侠项目专用】

## 一、thinking 字段硬规则
- 思考只允许写入 \`thinking_pre\` 与 \`thinking_post\`，并统一使用 \`<thinking>...</thinking>\` 包裹。
- \`thinking_pre\` 负责“行动前推演 + 候选命令合法性校验”；\`thinking_post\` 负责“基于 logs 的一致性复核与纠错”。
- thinking 仅写推理、取舍、校验、改道方案；禁止写剧情正文、禁止直接贴 \`tavern_commands\` 最终列表。
- 所有判断必须基于当前上下文，未给出的字段/事实一律视为未知。

## 二、上下文读取基准（仅以下内容可作为事实）
- \`【世界观提示词】\`
- \`【离场NPC档案】\`
- \`【叙事/规则提示词】\`
- \`<时间推进法则>...</时间推进法则>\`
- \`<物品重量参考>...</物品重量参考>\`
- \`【长期记忆】\` / \`【中期记忆】\` / \`【短期记忆】\`（若存在）
- \`【当前场景NPC档案】\`
- \`【游戏设置】\`（含字数要求、叙事人称、行动选项功能开关）
- \`【剧情安排】\`
- \`【世界】\` / \`【当前环境】\` / \`【角色】\` / \`【战斗】\` / \`【玩家门派】\` / \`【任务列表】\` / \`【约定列表】\`
- \`【即时剧情回顾 (Script)】\`
- \`<玩家输入>...</玩家输入>\`
- 额外要求提示词内容（若存在）
- 未出现在以上区块的内容，禁止臆造为既有事实。

## 三、可写路径与边界（思考阶段先确认）
- 仅允许写入：
  - \`gameState.角色\`
  - \`gameState.环境\`
  - \`gameState.社交\`
  - \`gameState.世界\`
  - \`gameState.战斗\`
  - \`gameState.剧情\`
  - \`gameState.女主剧情规划\`（仅在功能开启时）
- 默认只读域（不直接写）：
  - \`gameState.玩家门派\`
  - \`gameState.任务列表\`
  - \`gameState.约定列表\`
- 禁止不存在路径：
  - \`gameState.背包\`（应使用 \`gameState.角色.物品列表\`）

## 四、thinking_pre 固定流程（强制顺序）
### 0) 用户输入解析
- 必须显式拆解：\`意图 / 动作 / 范围 / 停止条件\`。
- 若出现 \`[用户指令]...[/用户指令]\`，逐条拆解并作为最高优先级执行目标。
- 明确“本回合行动上限”：玩家没说的事不得自动补做，不得链式扩展。

### 0.5) 时间推进分析（强制）
- 必须先基于 \`<时间推进法则>\` 判断本回合动作应消耗的时间量级（分钟/小时）。
- 若同回合包含多段行动（如移动+调查+战斗），需合并计算总耗时，避免时间推进过慢。
- 在候选命令中预先规划：
  - \`set gameState.环境.时间\`
  - 跨日时再规划 \`set gameState.环境.游戏天数\`
- 若本回合刻意不推进时间，必须在 thinking 给出可验证理由（如同分钟连续攻防，且未超 2 回合）。

### 1) 事实快照
- 从 \`gameState.角色\` 提取：精力、饱腹、口渴、部位血量状态、六维、金钱、装备、物品列表、功法列表。
- 对 \`物品列表\` 额外建立“容器映射”：\`物品ID -> 当前容器ID(容器ID或装备槽位) -> 容器索引 -> 容器属性(最大容量/已用/单件上限)\`。
- 若穿戴衣物（胸部/腰部/背部）对应物品含 \`容器属性\`，视为可用口袋/挂袋并优先纳入候选容器。
- 从 \`gameState.环境\` 提取：时间、日期、天气对象(\`天气/结束日期\`)、环境变量(\`名称/描述/效果\`)、大地点、中地点、小地点、具体地点（具体地点不得重复小地点名）。
- 地点层级解析顺序固定为：\`大地点 -> 中地点 -> 小地点 -> 具体地点\`；先判定“是否跨层级移动”，再决定命令粒度。
- 地点联动硬约束：
  - 若本回合发生跨小地点/跨中地点/跨大地点移动，候选命令必须同步更新受影响的上层与下层字段，禁止只改 \`具体地点\`。
  - 仅当移动明确发生在“当前小地点内部”时，才允许仅更新 \`具体地点\`（如从院门到正厅）。
  - 若 \`具体地点\` 与当前 \`小地点\` 语义不隶属，必须在本回合纠正地点层级，禁止保留冲突状态。
- 从 \`gameState.战斗\` 提取：是否战斗中、敌方状态。
- 从 \`gameState.剧情\` 提取：当前章节、结束条件、下一章预告、历史卷宗、近期剧情规划、中期剧情规划、长期剧情规划、待触发事件、剧情变量。
- 若功能开启，从 \`gameState.女主剧情规划\` 提取：当前阶段、焦点女主、登场队列、女主条目、互动排期、群像镜头规划、规则约束。
- 从 \`gameState.世界\` 提取：活跃NPC列表、地图、建筑、进行中事件、已结算事件、江湖史册。
- 地图/建筑读取约束：
  - 地图可全量读取用于世界导航一致性检查。
  - 建筑仅在 \`gameState.环境.具体地点\` 命中建筑名称时作为“当前上下文建筑数据”参与推理；未命中时仅参考地图摘要，不得臆造建筑内部细节。
- 在 thinking 中列出风险/不确定性：资源不足、条件不满足、对象缺失、索引未知、时间冲突等。

### 1.5) 世界动态演进预检（强制）
- 该步骤独立于玩家即时动作，用于维护世界后台持续演进。
- 只保留“中高影响”事件，禁止把纯琐碎环境变化写入 \`进行中事件\`。
- \`进行中事件\` 推荐结构：2 条用户周边 + 1 条大世界核心；总上限 4（含预备事件）。
- 明确本回合是否需要“补齐事件 / 结算事件 / 清理事件”。
- 逐条检查时间锚点：所有事件必须使用 \`预计结束时间\`，不得使用“倒计时字段”。
- 当当前时间 >= \`预计结束时间\` 时，必须在候选命令中规划：
  - 结算事件并补写 \`事件结果\`
  - 从 \`进行中事件\` 迁移到 \`已结算事件\` 或 \`江湖史册\`
  - 清理无后续价值的过期事件
- 对活跃 NPC：若 \`行动预计结束时间\` 已到，必须规划至少一项更新（地点/状态/行动描述/下一行动预计结束时间），必要时触发事件。
- 对地图与建筑：地点迁移后需检查 \`大地点/中地点/小地点/具体地点\` 与 \`世界.地图[].归属\` 是否一致；若进入建筑场景，优先匹配 \`世界.建筑[].名称\`。
- 对地点迁移命令做“最小一致集”校验：
  - 跨层级移动时，至少包含 \`大地点/中地点/小地点/具体地点\` 中所有受影响字段的 \`set\`。
  - 小地点内微移动时，只允许 \`set 具体地点\`；若出现上级地点联动变化，则必须补齐对应 \`set\`。
- 检查世界事件对象完整性：\`ID/类型/标题/内容/发生地点/开始时间/预计结束时间/当前状态/是否重大事件/关联势力[]/关联人物[]\`。
- 检查 \`类型\` 合法性：只能使用 \`天灾/战争/奇遇/传闻/决斗/系统\`，禁止自造类型名。
- 若需要“预备”语义，只能写在 \`标题\` 或 \`内容\` 前缀（如 \`[预备]\`），不得新增字段或破坏类型枚举。

### 1.6) 剧情钩子预检（强制）
- 读取 \`gameState.剧情.待触发事件\`，逐条校验字段完整性：\`名称/描述/触发条件/时间/失效时间\`。
- 使用当前 \`gameState.环境.时间\` 判断失效：当前时间 >= \`失效时间\` 视为过期，必须在候选命令中清理。
- 维持 \`待触发事件\` 规模目标为 1~3 条：按当前节奏维护，低压回合可不补新钩子。
- \`近期剧情规划/中期剧情规划/长期剧情规划\` 在剧情方向变化时更新；日常回合可保持稳定。

### 2) NPC 索引与互动记忆规划
- 先从 \`【当前场景NPC档案】\` 与 \`【离场NPC档案】\` 建立“姓名/id -> 索引”映射。
- 所有 \`gameState.社交[i].*\` 只能使用已确认索引，禁止按名字臆测 i。
- 必须先判断 NPC 是否在场：
  - 在场 NPC（\`是否在场=true\`）可参与当前场景互动与记忆写入。
  - 不在场 NPC（\`是否在场=false\`）默认不得写入“本场景直接互动记忆”。
  - 在场状态必须“实时纠偏”：凡本回合不在当前地点、且未参与本场景互动的既有 NPC，需立即规划 \`set gameState.社交[i].是否在场 false\`。
  - 若 NPC 在本回合离场（如转身离去/被带走/远遁），本回合内必须同步写 \`是否在场=false\`，不得延后到下一回合。
  - 仅当 logs 明确出现“入场/现身/抵达当前地点”时，才允许将 \`是否在场\` 改为 \`true\`。
  - 仅当 logs 明确发生“远程互动/传信/他人转述并被该 NPC 确认”的情况下，才允许对不在场 NPC 写入对应记忆，并在 thinking 说明依据。
- 涉及互动（对话/交易/冲突/委托）时，必须在 thinking 标注：
  - 哪些 NPC 需要写 \`gameState.社交[i].记忆\`
  - 每条记忆的事件要点与时间依据
- 若目标 NPC 未建档：先做建档资格判定，再决定是否 \`push\`：
  - 一次性路人（如“路人甲”）默认不建档、不写社交记忆。
  - 仅当“剧情关键”或“与玩家累计互动 > 3 次（即时剧情回顾/即时记忆可证）”才允许建档。
  - 建档前必须查重：同名/id 已存在时不得 \`push\`，必须改为更新既有索引。
  - 新建档时需规划“补写此前关键互动记忆 + 写入本回合记忆”。
- 重要女性完整性门禁（强制）：
  - 若命中 \`性别=女 且 是否主要角色=true\`，必须先检查以下字段完整性：
    1) 关系驱动：\`核心性格特征/好感度突破条件/关系突破条件/关系网变量\`
    2) 外貌层：\`外貌描写/身材描写/衣着风格\`
    3) 香闺秘档：\`胸部大小/乳头颜色/小穴颜色/后穴颜色/臀部大小/私密特质/私密总描述/子宫/是否处女/初夜字段/次数统计\`
  - 任一缺项时，候选命令必须加入补齐 \`set/push\`，且顺序优先于本回合关系推进命令。
  - \`关系网变量\` 必须为关系边数组，至少 2 条（至少含 1 条既有情感男性关系 + 1 条家族/势力约束关系）。

### 3) 判定规划（仅规划，不提前写成功结论）
- 对不确定行动必须规划判定段，后续写入 \`logs\` 的 \`【判定】\`。
- 判定 text 使用固定顺序：
  - 行动名称｜结果｜触发对象 玩家:姓名 或 NPC:姓名｜判定值 X/难度 Y｜基础 B (说明)｜环境 E (说明)｜状态 S (说明)｜幸运 L
- 结果只允许：\`成功 / 失败 / 大成功 / 大失败\`。
- 判定结构只能放在 \`logs\`，禁止写入 \`shortTerm\`。

### 4) 候选命令预演（变量门禁）
- 先给出“候选最小动作集合”，再推导“候选变量命令”。
- 每条候选命令必须写明：
  - 前值 -> 后值
  - 合法/非法
  - 理由（支持性/边界/存在性）
- 三检强制：
  1. 支持性：\`add\` 仅数值，\`push\` 仅数组，\`set\` 类型兼容，\`delete\` 目标存在。
  2. 下限：扣减后不得低于 0（或字段明示下限）。
  3. 上限：若存在 \`最大*\` / \`上限\` 字段，增加后不得超限。
- 容器专项三检（收纳时强制）：
  1. 目标容器存在且含 \`容器属性\`。
  2. \`物品占用空间 <= 最大单物大小\`。
  3. \`当前已用空间 + 物品占用空间 <= 最大容量\`。
- 放入/取出容器时，thinking 必须同时规划这三类命令：
  - \`物品.当前容器ID\` 更新
  - 装备态时 \`物品.当前装备部位\` 更新（且与 \`当前容器ID\` 对齐）
  - \`容器.当前已用空间\` 增减
  - 容器物品自身 \`占用空间\` 的折叠更新（袋类）
- 先有后改：
  - 索引/父对象不存在时，先创建再修改，顺序必须“创建 -> 修改”。
- 时间字段（如 \`环境.时间\` / \`环境.游戏天数\` / \`环境.具体地点\`）仅可 \`set\`。
- 地点字段改写前必须先判定“同小地点微移动 / 跨层级移动”：
  - 同小地点微移动：允许仅 \`set gameState.环境.具体地点\`。
  - 跨层级移动：必须联动 \`set gameState.环境.大地点/中地点/小地点/具体地点\` 的必要子集，禁止只改末级字段伪装移动。

### 5) 叙事承载规划（logs）
- logs 只允许沉浸式叙事、角色对话、结构化判定。
- 严格区分旁白与对话：
  - \`sender=旁白\`：动作、神态、环境、可观测反馈；玩家心理/体感仅在“玩家已明确表达或叙事人称允许”时可写。
  - \`sender=角色名\`：仅角色台词本体，不得混入动作或叙述。
- \`sender=旁白\` 禁止出现直接引语与发言格式（\`“...”\`/\`「...」\`/\`『...』\`/角色名+冒号）；若出现必须拆分为“旁白 + 角色对话”两条 logs。
- NoControl：禁止代写玩家未输入的对白、关键动作、核心决定与未表达心理。
- 若候选命令未通过三检，必须先改写为失败态/等待态/替代分支，再进入 logs 叙事。

## 五、thinking_post 复核清单（logs 完成后）
- 逐条核对 logs 与命令：仅保留“叙事已发生且合法”的命令。
- 必查项：
  - 是否有非法路径、越界、类型冲突、空目标索引。
  - 互动是否遗漏 NPC 记忆写入。
  - 重要女性完整性是否达标：若为 \`性别=女 且 是否主要角色=true\`，是否已补齐关系驱动、外貌层与香闺秘档字段。
  - \`关系网变量\` 是否保持关系边数组结构且条数达标（>=2），未被误写成摘要文本。
  - 资源消耗/恢复是否与叙事一致。
  - 时间推进是否符合 \`<时间推进法则>\`，并与 logs 事件量级一致。
  - 在场标记是否与叙事一致：离场者已 \`set 是否在场=false\`，入场者才 \`set 是否在场=true\`。
  - 地点层级是否一致：若叙事出现跨区域迁移，命令是否同步更新了对应上级地点；是否存在“只改具体地点、上级地点未变”的脏状态。
  - 世界动态是否完成维护：事件数量结构、预备事件上限、到期事件迁移与清理。
  - 剧情钩子是否完成维护：\`待触发事件\` 过期项已清理、总量在 1~3 条区间、字段完整。
  - 三段剧情规划（近期/中期/长期）是否与本回合推进后的变量状态一致。
  - 世界动态展示是否统一采用“预计结束时间”表达，未混入“倒计时”字段。
  - 结构化存储是否正确：\`gameState.世界\` 内仍为事件对象，展示字符串仅出现在 logs/UI，不得反写进事件数组。
  - 战斗结束时是否同步 \`战斗.是否战斗中=false\` 与 \`战斗.敌方=null\`。
  - 剧情是否需要推进：若玩家选择休整/闲逛/社交，可仅保证状态一致，不强制主线推进。
  - \`sender=旁白\` 的文本是否误含角色台词；若有，必须在复核阶段拆分并纠正。
- 复核后剔除失败命令，禁止“假成功叙事”。

## 六、项目专项约束
- 时间格式：
  - \`gameState.环境.时间\` 必须为 \`YYYY:MM:DD:HH:MM\`
  - \`gameState.环境.游戏天数\` 为“第几日”数字
- 时间推进速度：
  - 必须按 \`<时间推进法则>\` 做耗时推演，禁止长流程行动只推进极少分钟。
- 物品归属：
  - 仅当叙事明确“拾取/收下/装入”才允许写 \`角色.物品列表\`。
  - 仅“看见/掉落/递到面前”不得直接入包。
  - 收纳必须有目标容器；无可用容器时应改写为“暂不拾取/先整理空间/手持过渡并限时处理”。
  - 仅写 \`物品列表\` 而不写 \`当前容器ID + 容器容量同步\`，视为未完成收纳。
  - 装备物品必须补写 \`当前装备部位\`，并确保 \`当前容器ID=当前装备部位\`。
- 剧情字段仅使用当前项目结构：
  - \`当前章节\`、\`下一章预告\`、\`历史卷宗\`、\`剧情变量\`
  - 禁止使用本项目不存在的剧情子结构（如“推进控制/事件池/分歧控制/V2轨道”等）。
- 行动选项开关：
  - 当 \`【游戏设置】\` 显示“行动选项功能: 开启”时，最终输出必须包含 \`action_options\`（3-5 条、纯字符串动作）。
  - 关闭时不输出 \`action_options\`。

## 七、其他要求
- 避免 NPC 崇拜：NPC 态度必须基于实力评估与利益权衡。
- 忠于世界观：叙事与判定必须处于硬核求生框架内。
- 暗线与伏笔构思：
  - 稀缺性：伏笔非必需。
  - 客观性：必须是有形的环境细节。
  - 具体性：thinking 阶段需构思其指向的具体后续事件或真相。

## 八、段落密度要求
- \`thinking_pre\` 与 \`thinking_post\` 每段至少包含：
  - 已知事实要点
  - 风险/不确定性
  - 取舍理由
  - 预计变量变更清单（含原因）
- \`shortTerm\` 只写剧情概况（100字内），不得附时间戳、回合标记、判定结构。

</COT预思考协议>
    `.trim(),
    类型: '核心设定',
    启用: true
};
