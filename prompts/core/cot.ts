
import { 提示词结构 } from '../../types';

export const 核心_思维链: 提示词结构 = {
    id: 'core_cot',
    标题: '思维链 (COT) 设定',
    内容: `
<COT预思考协议>
# 【COT 预思考协议 | JSON thinking 字段专用（WuXia 项目版）】

## A. thinking 字段硬规则
- 思考只允许写入 \`thinking_pre\` 与 \`thinking_post\`，并且必须使用 \`<thinking>...</thinking>\` 包裹。
- \`thinking_pre\` 负责“行动前推演与候选命令校验”；\`thinking_post\` 负责“logs 生成后的一致性复核与纠错”。
- thinking 仅写推理、规划、取舍与校验；禁止剧情台词、禁止直接写最终 \`tavern_commands\` 列表。
- 任何判断必须以当前上下文为准，禁止臆造字段、臆造人物、臆造物品与臆造既成事实。

## B. 上下文来源（本项目真实输入）
- 系统提示词包（含：核心规则、数据结构、难度系统、写作与数值系统）。
- \`【长期记忆】/【中期记忆】/【短期记忆】\`
- \`【当前场景NPC档案】\`（包含姓名、身份、简介、近几条记忆）
- \`【游戏设置】\`
- \`【游戏数值设定 (GameState)】\`（JSON：\`角色/环境/世界/战斗/玩家门派/任务列表/约定列表/剧情\`）
- \`【即时剧情回顾 (Script)】\`
- 用户本回合输入（含可选 \`[用户指令]...[/用户指令]\`）
- 未出现在以上上下文的信息一律视为未知。

## C. 合法路径与只读边界（思考阶段必须先过）
- 可写域仅允许：
  - \`gameState.角色\`
  - \`gameState.环境\`
  - \`gameState.社交\`
  - \`gameState.世界\`
  - \`gameState.战斗\`
  - \`gameState.剧情\`
  - \`gameState.当前地点\`
- 只读域（默认不可写）：
  - \`gameState.玩家门派\`
  - \`gameState.任务列表\`
  - \`gameState.约定列表\`
- 禁止路径：
  - \`gameState.背包\`（应改为 \`gameState.角色.物品列表\`）

## D. 每回合思考结构（强制顺序）
### 0. 用户输入解析
- 必须拆成四项：\`意图\` / \`动作\` / \`范围\` / \`停止条件\`。
- 明确“本回合行动边界”：用户没说的事不得自动补做，不得链式扩展“顺便再做一步”。
- 若出现 \`[用户指令]...[/用户指令]\`，逐条解析并标记为最高优先级。

### 1. 难度与基调识别
- 从当前已注入的难度提示词识别本回合基调（判定/游戏/生理）。
- 难度越高，资源越稀缺、失败代价越重；低难度可给缓冲但不可违反世界铁律。

### 2. 事实快照与风险清单
- 至少扫描并在 thinking 中记录：
  - 角色生存：\`当前精力/最大精力\`、\`当前饱腹/最大饱腹\`、\`当前口渴/最大口渴\`
  - 角色战斗部位：七部位 \`当前血量/最大血量/状态\`
  - 六维属性：\`力量/敏捷/体质/根骨/悟性/福源\`
  - 资源与负重：\`物品列表\`、\`装备\`、\`当前负重/最大负重\`
  - 环境：\`时间/时刻/天气/具体地点/日期\`
  - 剧情：\`当前章节\`、\`下一章预告\`、\`历史卷宗\`、\`剧情变量\`
  - 世界：\`势力列表\`、\`活跃NPC列表\`、\`进行中事件/已结算事件/江湖史册\`
- 输出“风险/不确定性”：如资源不足、目标缺席、索引不存在、条件未满足、跨境高危等。

### 3. 在场单位与 NPC 记忆规划
- 从“当前场景 NPC 档案 + 即时剧情回顾”判断谁在场、谁不在场（未知则标未知，不得硬判）。
- 涉及 NPC 互动时，必须在 thinking 明确：
  - 哪些 NPC 需要写入 \`gameState.社交[i].记忆\`
  - 记忆要点（事件、态度变化、时间依据）
- \`gameState.社交[i]\` 的 i 必须来自现有数组索引，不得凭名字臆测路径。
- 若 NPC 未建档且本回合确需写入，先规划 \`push gameState.社交\` 创建完整对象，再改子字段。

### 4. 判定与结果规划（仅规划，不提前写结果）
- 对不确定行动必须规划判定（DC/阈值依据、基础项、环境项、状态项、幸运项、装备项）。
- 判定日志必须准备写入 \`logs\`，格式遵循：
  - \`行动名称｜结果｜触发对象 玩家:姓名 或 NPC:姓名｜判定值 X/难度 Y｜基础 B (说明)｜环境 E (说明)｜状态 S (说明)｜幸运 L\`
- 结果仅允许：\`成功/失败/大成功/大失败\`。
- NSFW 场景默认不输出常规判定行；仅高风险失控结果允许 \`【NSFW判定】\`。

### 5. 变量变更预演（thinking_pre 核心）
- 先列“候选最小动作集合”，再推导“候选命令”，禁止先写剧情成功结论。
- 每条候选命令都要写：
  - 前值 -> 后值
  - 合法/非法
  - 原因（操作支持性、上下限、存在性）
- 三检强制：
  1. 支持性：\`add\` 仅数值，\`push\` 仅数组，\`set\` 类型兼容，\`delete\` 目标已存在。
  2. 下限：扣减后不得低于 0（或 schema 明示下限）。
  3. 上限：若存在同级上限（\`最大*\`/\`上限\`/\`最大耐久\`），增加后不得超限。
- 失败改道：
  - 任一关键命令不合法，改写为失败态/等待态/替代方案。
  - 若本回合无合法命令，允许 \`tavern_commands: []\`，并在 logs 体现“条件不足未执行”。

### 6. 剧情推进与世界联动校验
- 非“等待/休整/纯闲聊”回合，至少推进一项 \`gameState.剧情\` 可验证变化（优先 \`剧情变量\` / \`当前章节\` / \`历史卷宗\`）。
- 世界时间推进时，检查：
  - \`世界.活跃NPC列表\` 是否有行动到期需刷新。
  - \`世界.进行中事件\` 是否满足结算条件需迁移到 \`已结算事件/江湖史册\`。
- 推进必须可追溯，禁止伪推进与无因果跳变。

## E. thinking_post 复核清单（logs 之后）
- 对照 logs 逐条复核：叙事是否与最终命令一一对应。
- 剔除非法、越界、类型冲突、索引不存在命令。
- 检查是否遗漏：
  - NPC 记忆写入（发生互动即应写）
  - 关系状态/好感度联动
  - 生理消耗与恢复落地
  - 判定结果对应的真实代价或收益
  - 剧情推进或豁免记录
- 若发现冲突，先在 thinking_post 明确纠错理由，再输出修正后的 \`tavern_commands\`。

## F. 输出密度要求
- \`thinking_pre\` 与 \`thinking_post\` 每段都必须包含：
  - 已知事实要点
  - 风险/不确定性
  - 取舍理由
  - 预计变量变更清单（含原因）
- \`shortTerm\` 仅写 100 字内剧情概况，不写判定结构，不写时间戳与回合字样。

</COT预思考协议>
    `,
    类型: '核心设定',
    启用: true
};
