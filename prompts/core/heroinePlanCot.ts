import { 提示词结构 } from '../../types';

export const 核心_女主剧情规划_思考: 提示词结构 = {
    id: 'core_heroine_plan_cot',
    标题: '女主剧情规划思考',
    内容: `
<女主剧情规划思考协议>
# 【女主剧情规划思考协议｜独立附加思考模块】

## 0. 使用边界
- 本协议仅在“女主剧情规划功能开启”时附加生效。
- 本协议不替代主 COT，只补充“女主登场与互动编排”的专项思考。
- 不修改原有 \`thinking_pre/thinking_post\` 或 \`t_*\` 字段定义，只要求在既有思考中覆盖本协议检查项。

## 1. 回合前专项思考（Pre）
1. 登场态检查：
   - 读取 \`gameState.女主剧情规划.登场队列\` 与 \`女主条目\`；
   - 标记“可触发/未登场/已登场”分布，选出本回合主推进女主。
2. 关系态检查：
   - 对主推进女主核对 \`当前关系阶段/当前阶段目标/下一突破条件\` 是否一致；
   - 若不一致，先规划修正命令再规划推进命令。
3. 排期检查：
   - 读取 \`互动排期\`，筛选“待触发且未过期”候选；
   - 至少准备 A/B 两条互动走向，择一落地，另一条保留。
4. 群像检查：
   - 读取 \`群像镜头规划\`，判断本回合是否需要执行镜头以避免单线失衡。
5. 社交映射检查（强制）：
   - \`女主ID\` 必须映射到真实 \`gameState.社交[i].id\`；
   - 若映射失败，不得推进该女主关系阶段。

## 2. 正文后专项思考（Post）
1. 事件落地复核：
   - 正文发生的互动，是否对应到 \`女主条目\` 与 \`互动排期\` 的状态变化。
2. 时序复核：
   - \`最近推进时间\` 是否更新为本回合时间；
   - 过期排期是否已置为“已失效”或清理。
3. 联动复核：
   - 与 \`gameState.社交\` 的姓名/ID 是否保持一致；
   - 与 \`gameState.剧情.待触发事件\` 是否保持一致。
4. NTL专项复核（仅 NTL后宫）：
   - \`既有男性锚点\` 是否仍在；
   - \`阻断记录\` 是否补写关键节点；
   - 禁止把“关系逆转”写成无因果跳变。
5. 原剧情规划复核（强制）：
   - \`近期剧情规划/中期剧情规划/长期剧情规划\` 是否仍有有效内容；
   - 若本回合触发主线推进，是否同步维护原剧情规划字段而非只更新女主规划字段。

## 3. 命令输出纪律
- 优先级：修复一致性 > 推进关系阶段 > 更新排期状态 > 追加群像镜头状态。
- 若本回合无合法推进事件，允许只做“状态维持+清理过期排期”。
- 禁止把“计划中事件”当作“已发生事件”直接写入完成态。
- 禁止将女主规划字段写入 \`gameState.剧情.*\`；女主规划命令仅可落在 \`gameState.女主剧情规划.*\`。

</女主剧情规划思考协议>
    `.trim(),
    类型: '核心设定',
    启用: false
};
