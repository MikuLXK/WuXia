import { 提示词结构 } from '../../types';

export const 核心_女主剧情规划_思考: 提示词结构 = {
    id: 'core_heroine_plan_cot',
    标题: '女主剧情规划思考',
    内容: `
<女主剧情规划思考协议>
# 【女主剧情规划思考协议｜独立附加思考模块】

## 0. 使用边界
- 本协议仅在“女主剧情规划功能开启”时附加生效。
- 本协议不替代主 COT，只补充“女主登场与互动编排”的专项思考。
- 不修改原有 \`thinking_pre/thinking_post\` 或 \`t_*\` 字段定义，只要求在既有思考中覆盖本协议检查项。

## 1. 回合前专项思考（Pre）
1. 数据清洁预检（强制）：
   - 清理 \`登场队列/女主条目/互动排期/群像镜头规划\` 中空主键、重复主键、无映射 ID 条目；
   - 明确本回合“可保留 / 需清理”的条目列表，先清理后推进。
2. 登场态检查：
   - 读取 \`gameState.女主剧情规划.登场队列\` 与 \`女主条目\`；
   - 标记“可触发/未登场/已登场”分布，选出本回合主推进女主。
3. 在场性检查（强制）：
   - 主推进女主必须满足“可交互条件”（在场或有明确远程互动依据）；
   - 若不可交互，必须规划“拉回镜头”：切换 \`当前焦点女主ID\` 或转入群像镜头，不得硬推关系。
4. 关系态检查：
   - 对主推进女主核对 \`当前关系阶段/当前阶段目标/下一突破条件\` 是否一致；
   - 若不一致，先规划修正命令再规划推进命令。
5. 排期检查：
   - 读取 \`互动排期\`，筛选“待触发且未过期”候选；
   - 检查候选项 \`触发条件\` 是否仍与“当前阶段目标/下一突破条件”一致，不一致项需先改写或失效清理；
   - 至少准备 A/B 两条互动走向，择一落地，另一条保留。
6. 群像检查：
   - 读取 \`群像镜头规划\`，判断本回合是否需要执行镜头以避免单线失衡。
7. 社交映射检查（强制）：
   - \`女主ID\` 必须映射到真实 \`gameState.社交[i].id\`；
   - 若映射失败，不得推进该女主关系阶段。

## 2. 正文后专项思考（Post）
1. 事件落地复核：
   - 正文发生的互动，是否对应到 \`女主条目\` 与 \`互动排期\` 的状态变化。
2. 时序复核：
   - \`最近推进时间\` 是否更新为本回合时间；
   - 过期排期是否已置为“已失效”或清理。
3. 触发后清理复核（强制）：
   - 已触发/已失效排期是否在同回合完成清理（或有明确保留理由）；
   - \`已完成节点\` 与 \`待完成节点\` 是否完成对冲更新，避免重复挂账。
4. 镜头拉回复核（强制）：
   - 当本回合无合法推进事件时，是否已执行“焦点切换或群像承接”；
   - 禁止连续多回合保持同焦点且无任何变量变化。
5. 联动复核：
   - 与 \`gameState.社交\` 的姓名/ID 是否保持一致；
   - 与 \`gameState.剧情.待触发事件\` 是否保持一致；
   - 当女主阶段目标/突破条件改写后，剧情侧触发条件是否同步改写或清理。
6. NTL专项复核（仅 NTL后宫）：
   - \`既有男性锚点\` 是否仍在；
   - \`阻断记录\` 是否补写关键节点；
   - 禁止把“关系逆转”写成无因果跳变。
7. 原剧情规划复核（强制）：
   - \`近期剧情规划/中期剧情规划/长期剧情规划\` 是否仍有有效内容；
   - 若本回合触发主线推进，是否同步维护原剧情规划字段而非只更新女主规划字段。

## 3. 命令输出纪律
- 优先级：即时清理无用内容 > 修复一致性 > 推进关系阶段 > 更新排期状态 > 触发后清理 > 镜头拉回。
- 若本回合无合法推进事件，允许只做“状态维持+清理过期排期+镜头拉回”。
- 禁止把“计划中事件”当作“已发生事件”直接写入完成态。
- 已触发/已失效且无后续价值的排期，不得跨回合滞留在 \`互动排期\`。
- 禁止将女主规划字段写入 \`gameState.剧情.*\`；女主规划命令仅可落在 \`gameState.女主剧情规划.*\`。

</女主剧情规划思考协议>
    `.trim(),
    类型: '核心设定',
    启用: false
};
