import { 提示词结构 } from '../../types';

export const 核心_剧情推动: 提示词结构 = {
    id: 'core_story',
    标题: '剧情推动',
    内容: `
<剧情推动协议>
# 【剧情推动协议｜严格对齐 gameState.剧情 现有结构】

## 0. 目标
- 让每回合剧情推进“可追踪、可落地、可回溯”。
- 所有剧情推进必须映射到当前项目真实字段，禁止写入不存在的剧情子系统。

## 1. 仅允许使用的剧情路径
- \`gameState.剧情.当前章节\`
- \`gameState.剧情.当前章节.ID\`
- \`gameState.剧情.当前章节.序号\`
- \`gameState.剧情.当前章节.标题\`
- \`gameState.剧情.当前章节.背景故事\`
- \`gameState.剧情.当前章节.主要矛盾\`
- \`gameState.剧情.当前章节.结束条件\`
- \`gameState.剧情.当前章节.伏笔列表\`
- \`gameState.剧情.下一章预告\`（\`标题\` / \`大纲\`）
- \`gameState.剧情.历史卷宗\`
- \`gameState.剧情.剧情变量\`

### 禁止字段（当前结构不存在）
- 禁止写入：\`推进控制\` / \`事件池\` / \`轨道状态\` / \`目标面板\` / \`分歧控制\` 等任意未定义字段。

## 2. 回合推进判定流程（强制）
1. 从玩家输入提炼“本回合是否触发剧情推进”：
   - 纯闲聊/纯观察/纯等待：允许只微调 \`剧情变量\` 或不推进。
   - 明确行动/冲突/调查/抉择：必须产生至少 1 个剧情字段变化。
2. 先更新“事实变量”，再写“章节语义”：
   - 先 \`set gameState.剧情.剧情变量.xxx\`
   - 后按需要更新 \`当前章节\` 的矛盾、伏笔、结束条件达成态势
3. 若触发章节收束，再执行“卷宗归档 + 下一章预告修订”。

## 3. 结束条件判定（按类型）
- 时间型：比对 \`gameState.环境.时间\` 与 \`结束条件[].判定值\`（时间字符串需 \`YYYY:MM:DD:HH:MM\`）。
- 变量型：比对 \`gameState.剧情.剧情变量[对应变量键名]\` 是否满足 \`判定值\`。
- 事件型：仅当本回合 logs 已实际发生对应事件，才可判定达成。

## 4. 字段级更新策略
- 轻推进（多数回合）：
  - 优先 \`set gameState.剧情.剧情变量.xxx\`
  - 必要时 \`push gameState.剧情.当前章节.伏笔列表\`（新增线索）
- 中推进（章节中段）：
  - \`set gameState.剧情.当前章节.主要矛盾\`（矛盾升级/转向）
  - \`set gameState.剧情.当前章节.背景故事\`（仅在信息实质新增时）
- 重推进（章节收束）：
  - \`push gameState.剧情.历史卷宗\` 归档本章结语
  - \`set gameState.剧情.下一章预告.标题\` / \`set gameState.剧情.下一章预告.大纲\`
  - 如需换章，\`set gameState.剧情.当前章节\` 时必须提供完整章节对象（ID/序号/标题/背景故事/主要矛盾/结束条件/伏笔列表）。

## 5. 指令示例（合法）
- 推进变量：
\`{"action":"set","key":"gameState.剧情.剧情变量.是否救助神秘人","value":true}\`
- 更新章节矛盾：
\`{"action":"set","key":"gameState.剧情.当前章节.主要矛盾","value":"救人后引来的追兵已逼近村口，留守与撤离成为立即抉择。"}\`
- 增加伏笔：
\`{"action":"push","key":"gameState.剧情.当前章节.伏笔列表","value":"追兵腰牌上刻有归元宗外门印记"}\`
- 卷宗归档：
\`{"action":"push","key":"gameState.剧情.历史卷宗","value":{"标题":"第一卷：风起稻香","结语":"你在火光中做出离村决定，旧日平静自此终结。"}}\`
- 修订下一章预告：
\`{"action":"set","key":"gameState.剧情.下一章预告.标题","value":"第二卷：青牛暗潮"}\`

## 6. 一致性硬约束
- logs 没有发生的剧情结果，禁止提前写入 \`历史卷宗\`。
- 未满足结束条件，禁止强行切章。
- \`剧情变量\` 键名要稳定复用，不要同义重复造键（如“已救人/是否救助神秘人”并存）。
- 章节切换后，旧章不再写入 \`当前章节\`，后续信息应写入新章或 \`历史卷宗\`。

</剧情推动协议>
    `.trim(),
    类型: '核心设定',
    启用: true
};

