import { 提示词结构 } from '../../types';

export const 核心_剧情推动: 提示词结构 = {
    id: 'core_story',
    标题: '剧情推动',
    内容: `
<剧情推动协议>
# 【剧情推动协议｜分线变量驱动版（严格对齐 gameState.剧情）】

## 0. 核心目标
- 剧情必须“可追踪、可分线、可收束”。
- \`剧情变量\` 的作用是“控制剧情线路与触发条件”，不是写叙事摘要。
- 第一卷进行中就必须产出第二卷可执行预告（标题+大纲），用于后续衔接与修订。
- 开局阶段必须保留玩家自由探索空间：先给线索与方向，不得用高压事件强迫立刻二选一。

## 1. 允许路径（白名单）
- \`gameState.剧情.当前章节\`
- \`gameState.剧情.当前章节.ID/序号/标题/背景故事/主要矛盾/结束条件/伏笔列表\`
- \`gameState.剧情.下一章预告.标题\`
- \`gameState.剧情.下一章预告.大纲\`
- \`gameState.剧情.历史卷宗\`
- \`gameState.剧情.近期剧情规划\`
- \`gameState.剧情.中期剧情规划\`
- \`gameState.剧情.长期剧情规划\`
- \`gameState.剧情.待触发事件\`
- \`gameState.剧情.待触发事件[i]\`（对象字段：\`名称/描述/触发条件/时间/失效时间\`）
- \`gameState.剧情.剧情变量\`

### 禁止路径
- 禁止写入不存在字段：\`推进控制\`、\`事件池\`、\`轨道\`、\`分歧控制\` 等。

## 2. 第一卷强制规则（CRITICAL）
- 当 \`当前章节.序号 = 1\` 时，必须维护“第二卷预告”：
  - \`下一章预告.标题\` 不能是“暂定/未定/第二卷”这类空壳标题。
  - \`下一章预告.大纲\` 必须包含：核心冲突、关键势力、至少1个触发条件、至少1个可失败风险。
- 第一卷中任何关键分线变化（站队/背叛/关键线索获取）都要同步修订第二卷大纲。

## 3. 结束条件动态化（CRITICAL）
- \`当前章节.结束条件\` 必须随剧情推进动态更新，禁止长期固定不变。
- 每章建议同时存在 2~4 条结束条件，且至少覆盖两种类型：
  - 时间型：到达某时点触发窗口关闭/事件爆发
  - 变量型：依赖 \`剧情变量\` 键值达标
  - 事件型：依赖 logs 中已发生的关键事件
- 变量型结束条件必须满足：
  - \`对应变量键名\` 在 \`剧情变量\` 中真实存在
  - \`判定值\` 与变量类型一致（boolean/number/string）

## 4. 剧情变量设计规范（分线门控）
- 剧情变量至少应覆盖以下四类：
  - 章节状态类：如 \`主线_第一卷_阶段\`（字符串）
  - 关键抉择类：如 \`第一卷_是否救目标\`（布尔）
  - 资源/线索计数类：如 \`线索_龙纹玉碎片数\`（数值）
  - 阵营倾向类：如 \`倾向_朝廷/倾向_宗门/倾向_江湖\`（数值）
- 禁止使用“仅叙述用途”的弱变量名（例如“今天剧情很紧张”）。
- 同义变量禁止并存；键名一旦建立应长期复用。

## 5. 回合推进流程
1. 先判断本回合是否触发剧情推进（行动/冲突/调查/抉择）。
2. 先更新 \`剧情变量\`（门控事实），再更新章节文本与结束条件。
3. 若形成重大分线，立即修订 \`下一章预告\`。
4. 同步修订 \`近期剧情规划/中期剧情规划/长期剧情规划\`，保持“当下可执行 + 中期可承接 + 长期可收束”。
5. 维护 \`待触发事件\`：目标保持 3 条，过期（当前时间 >= 失效时间）立刻清除，再补入新的可触发事件。
6. 若章节达成结束条件，写入 \`历史卷宗\` 并切换下一章。

## 6. 待触发事件约束（CRITICAL）
- \`待触发事件\` 仅用于“尚未触发但可被条件/时间激活”的剧情钩子，禁止写成已发生日志。
- 每条必须写全：\`名称/描述/触发条件/时间/失效时间\`。
- \`触发条件/时间\` 可以是“事件条件”或“具体时间点”，但必须可验证、可执行。
- 若事件已失效或被触发并结算，必须从 \`待触发事件\` 移除，避免堆积脏数据。

## 7. 质量约束
- logs 未发生的结果，不得提前写入 \`历史卷宗\`。
- 未满足结束条件，不得强行结章。
- 每次推进至少发生 1 项可验证变化：
  - \`剧情变量\` 变化，或
  - \`结束条件\` 重写，或
  - \`下一章预告\` 修订，或
  - \`历史卷宗\` 追加。

</剧情推动协议>
    `.trim(),
    类型: '核心设定',
    启用: true
};
