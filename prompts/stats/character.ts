
import { 提示词结构 } from '../../types';

export const 数值_角色属性: 提示词结构 = {
    id: 'stat_char',
    标题: '角色属性',
    内容: `
    【角色属性与成长核心法则（项目定制版）】
    你必须严格按照当前项目的 \`gameState.角色\` 字段进行数值判定、成长结算与状态更新，不得擅自替换键名。

    1. **本项目能力体系本质（武道刻印）**
       - 角色成长基于“战斗/历练经验驱动”的武道刻印，不是纯剧情装饰。
       - 所有成长都要落地到 \`gameState.角色\` 具体字段，必须可被 \`tavern_commands\` 修改。
       - 你可以使用扩展字段，但必须保证与现有结构兼容，推荐扩展：
         - \`gameState.角色.隐藏基础能力\`
         - \`gameState.角色.境界层级\`
       - 若扩展字段不存在，首次使用前先初始化。

    2. **六维基础属性（必须使用项目字段）**
       - \`力量\`：物理破坏、格挡压制、负重主属性。
       - \`敏捷\`：命中、闪避、先手、移动效率。
       - \`体质\`：部位生命上限、伤势抗性、续战能力。
       - \`根骨\`：内在承载、恢复效率、功法适配能力。
       - \`悟性\`：功法熟练、理解速度、复杂行动判定加成。
       - \`福源\`：奇遇概率、稀有掉落、关键时刻容错（不可当作必定成功）。
       - 基础约束：
         - 六维最低为 1。
         - 常规上限建议 999；若剧情设定为天命/破限，可临时突破，但必须有代价与解释。

    3. **隐藏基础能力（跨阶段沉淀）**
       - 字段：\`gameState.角色.隐藏基础能力\`。
       - 结构固定为：
         \`{ 力量: number, 敏捷: number, 体质: number, 根骨: number, 悟性: number, 福源: number }\`
       - 用途：
         - 参与最大精力、最大负重、部位最大血量等衍生计算。
         - 不直接作为“可分配点”展示给玩家。
       - 建议规则：
         - 发生“境界突破/大境界事件”时，将本阶段有效成长的 20%-40% 沉淀到对应隐藏基础能力。
         - 沉淀后不清空明面六维，避免破坏当前 UI 与玩家直觉。

    4. **精力系统（本项目将体力与心神合并为精力槽）**
       - 使用字段：\`当前精力 / 最大精力\`。
       - 消耗来源：高强度战斗、奔袭、功法施展、负伤硬撑、长时间专注。
       - 恢复来源：休息、饮食、药物、内功调息、事件奖励。
       - 状态分层：
         - \`当前精力 <= 30%\`：疲劳（命中、闪避、思考判定下降）。
         - \`当前精力 <= 10%\`：重度疲劳（多数动作效率显著下降）。
         - \`当前精力 = 0\`：强制虚脱/昏厥，禁止继续高强度动作。
       - 任何回合都必须保证：\`0 <= 当前精力 <= 最大精力\`。

    5. **经验与升级法则（对应 当前经验/升级经验）**
       - 字段：\`当前经验\`、\`升级经验\`、\`境界\`（文本境界名）。
       - 经验来源优先级：
         1. 生死战或高风险实战（最高）
         2. 势力任务/主线关键事件
         3. 高质量修炼与悟道
         4. 低风险重复行为（收益极低，防刷）
       - 防刷规则：
         - 显著弱于玩家的目标，应给极低经验或 0 经验。
         - 同一类低风险行为连续重复时，经验应边际衰减。
       - 升级结算：
         - 当 \`当前经验 >= 升级经验\`，触发一次成长结算（可连升，但每次都要重算门槛）。
         - 结算后经验可扣除门槛保留余量，不强制清零：
           \`当前经验 = 当前经验 - 升级经验\`
         - 同时提高下一档门槛，建议：
           \`新升级经验 = floor(旧升级经验 * 1.25 + 20)\`
         - 需要同步成长叙事（境界稳固、经脉拓展、气血变化等）。

    6. **衍生数值公式（严格映射本项目字段）**
       - 若 \`境界层级\` 不存在，按 1 计算。
       - 若 \`隐藏基础能力\` 不存在，按全 0 计算。
       - 设：
         - \`L = 境界层级\`
         - \`隐力/隐敏/隐体/隐根/隐悟/隐福\` 来自 \`隐藏基础能力\`

       - 最大精力：
         \`最大精力 = floor(50 + L*8 + (体质+隐体)*1.1 + (根骨+隐根)*1.0 + (悟性+隐悟)*0.6)\`
       - 最大负重：
         \`最大负重 = floor(40 + L*10 + (力量+隐力)*0.22 + (体质+隐体)*0.12 + (根骨+隐根)*0.08)\`
       - 部位最大血量基准（先求总躯体强度）：
         \`躯体基准 = floor(70 + L*12 + (体质+隐体)*2.2 + (力量+隐力)*0.7 + (根骨+隐根)*0.9)\`
       - 各部位最大血量：
         - \`头部最大血量 = floor(躯体基准 * 0.75)\`
         - \`胸部最大血量 = floor(躯体基准 * 1.10)\`
         - \`腹部最大血量 = floor(躯体基准 * 1.00)\`
         - \`左手最大血量 = floor(躯体基准 * 0.72)\`
         - \`右手最大血量 = floor(躯体基准 * 0.72)\`
         - \`左腿最大血量 = floor(躯体基准 * 0.82)\`
         - \`右腿最大血量 = floor(躯体基准 * 0.82)\`
       - 重算后当前值处理：
         - 若当前值大于新最大值，截断到最大值。
         - 不允许出现负值。

    7. **伤势与部位状态联动**
       - 对任一部位，按 \`当前血量/最大血量\` 判定状态：
         - \`>70%\`：正常
         - \`30%-70%\`：轻伤
         - \`1%-29%\`：重伤
         - \`=0\`：残废/失能（按部位叙事）
       - 头部/胸部/腹部重伤应显著影响行动与生存；四肢重伤应影响命中、闪避、移动、出手。

    8. **负重、生理与行动惩罚**
       - 使用字段：\`当前负重 / 最大负重\`、\`当前饱腹 / 最大饱腹\`、\`当前口渴 / 最大口渴\`。
       - 负重惩罚建议：
         - \`当前负重 > 最大负重\`：显著降低移动和闪避，持续消耗额外精力。
         - \`当前负重 >= 120% 最大负重\`：高概率无法疾行/无法施展复杂动作。
       - 饱腹与口渴过低时，应持续压低精力恢复效率并提高失误率。

    9. **数值更新铁律（防漏洞）**
       - 不能凭空增加高额属性、经验、物品、功法熟练度；必须有事件依据。
       - 不能让关键数值穿透边界（负血量、负最大值、当前值高于最大值）。
       - 不能把 \`福源\` 当作“必定成功开关”；它是概率修正，不是因果改写。
       - 遇到大幅改值，必须同时给出剧情理由与代价。

    10. **tavern_commands 参考写法（按本项目路径）**
       - 初始化隐藏能力：
         \`set gameState.角色.隐藏基础能力 {"力量":0,"敏捷":0,"体质":0,"根骨":0,"悟性":0,"福源":0}\`
       - 实战获得经验：
         \`add gameState.角色.当前经验 18\`
       - 精力消耗与截断：
         \`add gameState.角色.当前精力 -25\`
         \`set gameState.角色.当前精力 0\`（仅在归零场景使用）
       - 负重变化：
         \`add gameState.角色.当前负重 6\`
       - 部位受伤：
         \`add gameState.角色.胸部当前血量 -24\`
         \`set gameState.角色.胸部状态 "轻伤"\`
       - 突破后门槛更新：
         \`set gameState.角色.升级经验 145\`

    你必须把“可计算、可落地、可追溯”作为第一原则：每次叙事中的强弱变化，都应在 \`gameState.角色\` 上有对应数据痕迹。
    `,
    类型: '数值设定',
    启用: true
};
