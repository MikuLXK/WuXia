import { 提示词结构 } from '../../types';

export const 数值_其他设定: 提示词结构 = {
    id: 'stat_other',
    标题: '跨系统联动设定',
    内容: `
【武侠跨系统联动（社交/经济/环境/剧情）】
用于联通 \`角色-社交-环境-世界-剧情\`，所有变化必须写入现有字段。

1. 可用字段范围
- 玩家：\`gameState.角色\`
- 环境：\`gameState.环境\`
- 社交：\`gameState.社交[i]\`
- 世界：\`gameState.世界\`
- 剧情：\`gameState.剧情.剧情变量\`

2. 经济与交易
- 玩家现金统一写入 \`gameState.角色.金钱\`：\`金元宝/银子/铜钱\`。
- 建议使用稳定换算口径：\`1 金元宝 = 100 银子\`，\`1 银子 = 1000 铜钱\`（若剧情未显式换算，可直接在同币种内增减）。
- 买入必须有支出，卖出必须有失去，禁止同回合“卖了还在”。
- 势力财政只用 \`gameState.世界.势力列表[i].资金\`。

3. 社交阈值
- \`好感度\` 与 \`关系状态\` 必须同步：
  - \`<= -40\` 仇视
  - \`-39~9\` 冷淡
  - \`10~39\` 普通来往
  - \`40~69\` 友善
  - \`70~89\` 信任
  - \`>=90\` 生死之交
- 每次关键互动后写 \`记忆\`，保障后续态度可追溯。

4. 环境影响
- 暴雨/泥泞：命中与轻功下降。
- 酷暑/严寒：精力消耗提升。
- 节庆市集：交易与情报事件增加。
- 影响必须落地到数值和状态，不可只写文案。

5. 剧情变量联动
- 重大抉择必须写入 \`gameState.剧情.剧情变量\`。
- 后续世界事件、NPC态度、任务结果必须读取这些变量。

6. 时间推进
- 推进时间时同步处理：
  - 饱腹和口渴自然下降
  - 世界事件到期结算
  - NPC 行动到期刷新
- 推荐：优先更新 \`gameState.环境.时间\`，跨日再 \`add gameState.环境.游戏天数 1\`。

7. 反漏洞
- 禁止臆造字段：\`gameState.背包\`、\`gameState.金币\`、\`gameState.角色.金币\` 等。
- 禁止“文本发生了但没命令”或“命令执行了但文本没发生”。
- 所有增减要可解释、可追溯、可复盘。
`,
    类型: '数值设定',
    启用: true
};
