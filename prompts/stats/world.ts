import { 提示词结构 } from '../../types';

export const 数值_世界演化: 提示词结构 = {
    id: 'stat_world_evo',
    标题: '世界演化法则',
    内容: `
<世界演化协议>
【武侠世界演化引擎】
世界推进由“活跃NPC后台行动 + 世界事件链 + 时间流逝”共同驱动，禁止无因果跳变。

1. 时间铁律
- 所有结构化时间统一 \`YYYY:MM:DD:HH:MM\`。
- 适用字段：\`行动开始时间 / 行动预计结束时间 / 开始时间 / 预计结束时间 / 消逝时间\`。
- 描述文本可用“子时/卯时”等古风表达，但变量字段必须保持标准时间戳。

2. 影响层级筛选（只保留中高影响）
- 仅将“会改变后续局势”的动态写入 \`gameState.世界\`：
  - 关键 NPC 动向变化
  - 跨区域冲突、奇遇、灾害、庆典
  - 关键资源波动、江湖秩序变化、对玩家后续路线有实质影响的信息
- 禁止把纯日常噪声写入世界事件（如普通天气波动、无后果闲谈）。

3. 活跃NPC后台推进（字段一致性）
- 每个 \`gameState.世界.活跃NPC列表[i]\` 必须维持：
  - \`当前位置\`
  - \`状态\`
  - \`当前行动描述\`
  - \`行动开始时间\`
  - \`行动预计结束时间\`
- 当当前时间到达/超过 \`行动预计结束时间\` 时，必须至少更新一项：地点、状态、行动描述、下一次行动预计结束时间，必要时触发世界事件。

4. 世界事件流管理（核心）
- 活跃事件写入：\`gameState.世界.进行中事件\`
- 结算事件写入：\`gameState.世界.已结算事件\`
- 重大历史归档：\`gameState.世界.江湖史册\`
- 创建事件：\`push gameState.世界.进行中事件 {完整事件对象}\`
- 事件对象至少包含：
  - \`ID/类型/标题/内容/发生地点\`
  - \`开始时间/预计结束时间\`
  - \`当前状态("进行中"|"已结算")\`
  - \`是否重大事件\`
  - \`关联势力[]/关联人物[]\`（字段必须存在，可为空数组）
- 事件 \`类型\` 只能是：\`天灾/战争/奇遇/传闻/决斗/系统\`，禁止自造枚举。
- 到期后必须补写 \`事件结果\`，再迁移至 \`已结算事件\` 或 \`江湖史册\`。

5. 动态结构与数量约束
- \`gameState.世界.进行中事件\` 推荐结构：\`2 条用户周边 + 1 条大世界核心\`。
- 常态数量建议 3 条；含“预备事件”时总上限 4 条。
- 预备事件仅限“已启动但未落地结果”的关键动向（如 NPC 已得密报、NPC 已出发接触玩家等）。
- 预备事件是“语义标记”而非新字段：
  - \`类型\` 仍必须使用既有枚举（天灾/战争/奇遇/传闻/决斗/系统）；
  - 需要标注时在 \`标题\` 或 \`内容\` 前缀 \`[预备]\`。
- 若当前动态不足，需主动补齐；若超限，优先结算低影响、低关联、已临近结束的事件。
- 禁止使用空事件凑数。

6. 时间触发与清理
- 所有事件/行动时间统一 \`YYYY:MM:DD:HH:MM\`。
- 事件的结束判断以 \`预计结束时间\` 为准，不使用“倒计时”字段。
- 当前时间达到事件 \`预计结束时间\` 后：
  - 先生成 \`事件结果\`；
  - 再将事件从 \`进行中事件\` 迁移到 \`已结算事件\`，并将 \`当前状态\` 设为 \`已结算\`；
  - 若 \`是否重大事件=true\`，可额外归档到 \`江湖史册\`（归档对象也应保持 \`当前状态=已结算\`）；
  - 对无后续价值的旧事件进行清理，避免列表堆积。

7. 玩家可见联动
- 世界变化必须在可感知层落地：
  - 环境层：\`gameState.环境\`（天气对象/环境变量/节日/地点）
  - 角色层：\`gameState.角色.玩家BUFF\` 或资源状态变化
- 禁止“只改世界变量，不让玩家感知到变化”。

8. 命令操作纪律
- \`进行中事件\` 到期结算推荐顺序：
  1) \`set 进行中事件[i].事件结果\`
  2) \`set 进行中事件[i].当前状态 "已结算"\`
  3) \`push 已结算事件\` 或 \`push 江湖史册\`
  4) \`delete 进行中事件[i]\`
- 修改 \`活跃NPC列表\` 时，确保同一 NPC 在同一时间只处于一个地点和一个主要行动。

9. 一致性底线
- 禁止时间倒流。
- 禁止事件长期悬挂不结算。
- 禁止同一人物在同一时刻出现在互斥地点执行互斥行为。
- 禁止“只改变量不反映叙事”或“只写叙事不落地变量”。

10. 命令示例
- \`set gameState.世界.活跃NPC列表[0].行动预计结束时间 "1024:03:03:12:00"\`
- \`set gameState.世界.进行中事件[0].预计结束时间 "1024:03:03:14:30"\`
- \`set gameState.世界.进行中事件[0].当前状态 "已结算"\`
- \`push gameState.世界.已结算事件 {完整事件对象}\`
- \`delete gameState.世界.进行中事件[2]\`
</世界演化协议>
`,
    类型: '数值设定',
    启用: true
};
