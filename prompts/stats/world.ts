import { 提示词结构 } from '../../types';

export const 数值_世界演化: 提示词结构 = {
    id: 'stat_world_evo',
    标题: '世界演化法则',
    内容: `
【武侠世界演化引擎】
世界推进必须由“势力博弈 + 活跃NPC行动 + 时间流逝”驱动，禁止无因果随机跳变。

1. 时间铁律
- 结构化时间统一 \`YYYY:MM:DD:HH:MM\`。
- 适用：\`行动开始时间/行动预计结束时间/开始时间/预计结束时间/消逝时间\`。
- 描述可用子时卯时，但字段必须是标准时间戳。

2. 势力驱动
- \`势力列表\` 的 \`声望/资金/当前状态/当前战略\` 决定事件方向。
- 外交关系变化必须有事件依据，不得无理由突变。

3. 活跃NPC后台
- 每个 \`活跃NPC\` 必须维护：\`当前位置/当前行动描述/行动预计结束时间\`。
- 到时后必须更新至少一项：地点、行动、结束时间、触发事件。
- 持有重宝的 NPC 更易触发争夺、追杀、结盟与奇遇。

4. 事件生命周期
- 创建：\`push gameState.世界.进行中事件 {完整事件对象}\`
- 进行中事件完整对象至少包含：
  - \`ID/类型/标题/内容/发生地点\`
  - \`开始时间(YYYY:MM:DD:HH:MM)\`
  - \`预计结束时间(YYYY:MM:DD:HH:MM)\`  // 必填，作为结算触发锚点
  - \`当前状态("进行中"|"已结算")\`
  - \`是否重大事件/关联势力[]/关联人物[]\`
- 结算：\`当前状态="已结算"\` + \`事件结果\`
- 迁移：重大事件入 \`江湖史册\`；普通事件入 \`已结算事件\` 并到 \`消逝时间\` 清理。

5. 进行中事件数量硬约束
- \`gameState.世界.进行中事件\` 必须 \`>= 3\`。
- 推荐稳态 \`4~6\`，默认目标 \`5\`。
- 少于 3 必须补足；多于 6 应优先结算低影响事件。
- 禁止使用空事件凑数。

6. 玩家可见联动
- 世界变化必须反馈到玩家：
  - 环境层：\`gameState.环境.天气/节日/大地点/中地点/小地点/具体地点/环境描述\`
  - 角色层：\`gameState.角色.玩家BUFF\` 或资源波动
- 禁止只改世界不改玩家感知。

7. 一致性约束
- 禁止事件长期悬挂不结算。
- 禁止时间倒流。
- 禁止同人物同刻多地冲突。

8. 示例
- \`set gameState.世界.活跃NPC列表[0].行动预计结束时间 "1024:03:03:12:00"\`
- \`set gameState.世界.进行中事件[0].当前状态 "已结算"\`
- \`delete gameState.世界.进行中事件[2]\`
`,
    类型: '数值设定',
    启用: true
};
