
import { 提示词结构 } from '../../types';

export const 数值_世界演化: 提示词结构 = {
    id: 'stat_world_evo',
    标题: '世界演化法则',
    内容: `
    请严格遵循以下规则来模拟世界的动态演化：

    1. **严格时间格式 (YYYY:MM:DD:HH:MM)**:
       - 所有的事件时间、NPC行动时间必须使用 "1024:03:01:14:30" 这种格式。
       - 不要使用 "三月初一" 或 "午时" 等自然语言，除非是在《标题》或《内容》的描述文本中。

    2. **世界事件生命周期**:
       - **生成 (Start)**: 创建新事件放入 \`gameState.世界.进行中事件\`。
         * 必须设定具体的 \`预计结束时间\`。
       - **结算 (Result)**: 当游戏时间超过 \`预计结束时间\`：
         * 必须生成 \`事件结果\` (如: "某方获胜，导致...")。
         * 将状态改为 "已结算"。
         * 将事件从 \`进行中事件\` 移动到 \`已结算事件\`。
         * 设定 \`消逝时间\` (通常为结算后24小时)。
       - **消逝 (Vanish)**: 当时间超过 \`消逝时间\`：
         * 若 \`是否重大事件\` 为 true，移入 \`江湖史册\`。
         * 否则直接删除。

    3. **活跃NPC行动逻辑**:
       - 活跃NPC在后台通过 \`当前行动描述\` 和 \`行动预计结束时间\` 来模拟生活。
       - 当时间到达 \`行动预计结束时间\` 时，必须更新该NPC的：
         * \`当前位置\` (若移动)
         * \`当前行动描述\` (新的任务)
         * \`行动预计结束时间\` (新的截止点)
         * 可能伴随生成一个新的 \`世界事件\` (如: 到达某地引发了骚乱)。

    4. **数据操作指令示例**:
       - 生成事件: \`push gameState.世界.进行中事件 { "ID": "evt_new_01", "标题": "...", "开始时间": "1024:03:02:08:00", "预计结束时间": "1024:03:02:12:00", ... }\`
       - 结算事件: 
         1. \`set gameState.世界.进行中事件[Index].当前状态 "已结算"\`
         2. \`set gameState.世界.进行中事件[Index].事件结果 "结果描述..."\`
         3. (通常由前端或下一轮指令处理移动逻辑，AI需负责生成结果数据)

    5. **一致性**:
       - 事件的发生必须基于 \`势力列表\` 的战略和 \`活跃NPC\` 的动向，不要凭空生成无逻辑的随机事件。
    `,
    类型: '数值设定',
    启用: true
};
