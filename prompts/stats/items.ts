import { 提示词结构 } from '../../types';

export const 数值_物品属性: 提示词结构 = {
    id: 'stat_item',
    标题: '物品与装备系统',
    内容: `
<物品装备协议>
【物品与装备主规（严格匹配 <数据结构定义> 的物品字段）】
所有物品写入 \`gameState.角色.物品列表\`，装备引用写入 \`gameState.角色.装备\`。

1. 基础字段（全类型共用）
- \`ID, 名称, 描述, 类型, 品质, 重量, 占用空间, 价值, 当前耐久, 最大耐久, 词条列表\`
- 可选定位：\`当前容器ID?\`、\`当前装备部位?\`
- 统一单位：\`重量=斤\`（可小数）；\`占用空间=格\`（数值）。
- \`ID\` 必须唯一，禁止同实例重复出售后仍保留。

2. 类型扩展字段（按类型精确补齐）
- 武器：\`武器子类, 最小攻击, 最大攻击, 攻速修正, 格挡率\`
- 防具：\`装备位置, 覆盖部位, 物理防御, 内功防御\`
- 纯容器：\`装备位置(背部|腰部), 容器属性\`
- 秘籍：\`对应技能名, 学习要求, 研读进度, 是否消耗\`
- 消耗品：\`使用效果[], 毒性\`

3. 容器系统硬约束（强制）
- 容器字段仅允许：\`容器属性={最大容量, 当前已用空间, 最大单物大小, 减重比例}\`。
- 收纳时必须同时满足：
  - \`物品.占用空间 <= 容器.容器属性.最大单物大小\`
  - \`容器.当前已用空间 + 物品.占用空间 <= 容器.最大容量\`
- 写入顺序：
  1) \`set 物品.当前容器ID\`
  2) 必要时 \`set 物品.当前装备部位\`
  3) \`set/ add 容器.容器属性.当前已用空间\`
- 已装备物品必须满足：\`当前容器ID = 当前装备部位\`（如 \`主武器\`）。
- 允许袋中袋，但每层都要容量校验，禁止循环嵌套。

4. 负重与耐久
- \`gameState.角色.当前负重\` 必须与“携带+装备”一致。
- 超重（\`当前负重 > 最大负重\`）必须触发行动惩罚（命中/闪避/移动/精力消耗）。
- 耐久结算：
  - 战斗、格挡、恶劣环境会扣 \`当前耐久\`
  - \`当前耐久 <= 0\` 时必须衰减或失效
  - \`最大耐久=0\` 仅限极少数不可破坏物

5. 交易与归属
- 项目不存在 \`gameState.背包\` 顶层路径。
- 玩家物品只进 \`gameState.角色.物品列表\`；NPC 持有可进 \`gameState.社交[i].背包\`。
- “看到掉落”不等于“已入包”，必须有拾取/收纳动作。
- 装备栏可写名称或 ID，但对应完整物品对象必须在 \`物品列表\` 内存在。

6. 平衡与反漏洞
- \`重量/价值/耐久/攻防/概率\` 不得为负。
- 高品质（极品/绝世/传说）必须绑定风险、事件或势力后果。
- 稀有产出遵守 <掉落资源协议>，重量参考遵守 <物品重量参考>。
</物品装备协议>
`,
    类型: '数值设定',
    启用: true
};
