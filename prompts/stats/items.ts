
import { 提示词结构 } from '../../types';

export const 数值_物品属性: 提示词结构 = {
    id: 'stat_item',
    标题: '物品与装备系统',
    内容: `
    【武侠物品系统（严格对齐项目接口）】
    所有物品必须遵守 \`models/item.ts\` 的全中文结构，并与 \`gameState.角色.物品列表\`、\`gameState.角色.装备\` 联动。

    1. **基础字段（所有物品必备）**
       - 必备：\`ID, 名称, 描述, 类型, 品质, 重量, 占用空间, 价值, 当前耐久, 最大耐久, 词条列表\`。
       - \`ID\` 必须唯一，建议前缀：\`itm_\`、\`wpn_\`、\`arm_\`、\`book_\`。
       - \`词条列表\` 元素结构固定：\`{ 名称, 属性, 数值, 类型 }\`。

    2. **武侠世界观约束**
       - 品质阶：\`凡品 < 良品 < 上品 < 极品 < 绝世 < 传说\`。
       - 命名风格需符合江湖语境（如“青锋剑”“乌金软甲”“回春散”），禁止科幻命名。
       - 掉落和商店生成必须考虑玩家 \`境界\` 与当前区域危险度，避免早期发放失衡神装。

    3. **类型专属字段**
       - 武器（\`类型="武器"\`）必须有：
         - \`武器子类, 最小攻击, 最大攻击, 攻速修正, 格挡率\`
       - 防具（\`类型="防具"\`）必须有：
         - \`装备位置, 覆盖部位, 物理防御, 内功防御\`
       - 容器（\`类型="容器"\`）必须有：
         - \`装备位置("背部"|"腰部"), 容器属性\`
       - 秘籍（\`类型="秘籍"\`）必须有：
         - \`对应技能名, 学习要求, 研读进度, 是否消耗\`
       - 消耗品（\`类型="消耗品"\`）必须有：
         - \`使用效果[], 毒性\`

    4. **容器与负重规则**
       - \`容器属性\` 结构固定：
         - \`最大容量, 当前已用空间, 最大单物大小, 减重比例, 内容物\`
       - \`减重比例\` 仅影响“传导负重”，不代表物品无重量。
       - 任何获得/丢弃/装填行为都应同步考虑 \`gameState.角色.当前负重\` 变化。

    5. **耐久与破损规则**
       - 战斗、格挡、恶劣环境会消耗 \`当前耐久\`。
       - \`当前耐久 <= 0\` 时，装备应表现为“破损/难以发挥原性能”。
       - \`最大耐久=0\` 代表不可破坏（神兵或剧情道具），但数量应严格稀缺。

    6. **装备位映射（必须使用项目键名）**
       - 角色装备键：\`头部/胸部/腿部/手部/足部/主武器/副武器/暗器/背部/腰部/坐骑\`。
       - 防具 \`装备位置\` 与角色装备位需一致匹配。
       - 卸装后必须恢复为 \`"无"\` 或替换为新物品 ID/名称。

    7. **交易与归属**
       - 本项目无 \`gameState.背包\` 顶层，所有玩家物品都在 \`gameState.角色.物品列表\`。
       - NPC持有物由 \`gameState.社交[i].背包\` 维护（字符串列表）。
       - 叙事只写“看见/掉落”时，禁止直接加入玩家物品列表；必须明确“拾取/收入囊中”。

    8. **数值边界与反作弊**
       - \`占用空间\` 推荐 1~10；极端巨物可超 10，但必须有剧情合理性。
       - \`重量\`、\`价值\`、\`耐久\`、攻击防御相关数值不得为负。
       - 禁止无代价复制稀有道具，禁止同回合同时“出售后仍持有”。

    9. **生成标准**
       - 生成掉落、商店、任务奖励时，必须返回能直接写入 \`游戏物品\` 接口的 JSON 对象。
       - 不要输出半结构、伪字段或英文混搭键名。
    `,
    类型: '数值设定',
    启用: true
};
