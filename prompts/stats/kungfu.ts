
import { 提示词结构 } from '../../types';

export const 数值_功法体系: 提示词结构 = {
    id: 'stat_kungfu',
    标题: '功法体系',
    内容: `
    【武侠功法体系（扁平化强约束）】
    所有功法数据必须严格符合 \`models/kungfu.ts\`，并写入 \`gameState.角色.功法列表\`。

    1. **基础字段必须齐全**
       - 必须包含：
         - \`ID, 名称, 描述, 类型, 品质, 来源\`
         - \`当前重数, 最高重数, 当前熟练度, 升级经验, 突破条件\`
         - \`境界限制, 武器限制\`
         - \`消耗类型, 消耗数值, 施展耗时, 冷却时间\`
         - \`基础伤害, 加成属性, 加成系数, 伤害类型\`
         - \`目标类型, 最大目标数\`
         - \`附带效果, 被动修正, 境界特效\`
       - 禁止使用嵌套 \`combatConfig\` 等非项目结构。

    2. **武侠分类语义**
       - \`内功\`：续航、护体、恢复、真气驱动。
       - \`外功\`：拳脚兵刃、刚猛爆发。
       - \`轻功\`：位移、闪避、追击、脱战。
       - \`绝技\`：高门槛高收益，常伴随代价。
       - \`被动\`：常驻修正，通常写入 \`被动修正\`。

    3. **数值与成长平衡**
       - \`当前重数 <= 最高重数\`，\`当前熟练度 < 升级经验\` 时不可升重。
       - 练功与实战都可涨熟练，但高风险实战收益更高。
       - 品质越高，\`突破条件\` 应更苛刻（境界、悟性、奇遇或师承）。

    4. **消耗映射（基于当前项目资源）**
       - 当前项目稳定资源字段是 \`gameState.角色.当前精力\` 与部位血量。
       - 当 \`消耗类型="精力"\`：直接扣 \`当前精力\`。
       - 当 \`消耗类型="内力"\` 且无独立内力字段：默认折算为精力消耗，并在叙事中说明“以内息化力”。
       - 当 \`消耗类型="气血"\`：优先表现为高风险代价，可扣精力并附加部位轻伤，不得无代价。
       - 资源扣减后不得低于 0。

    5. **战斗结算建议**
       - 伤害基线建议：\`基础伤害 + (加成属性值 * 加成系数)\` 再叠加状态修正。
       - \`伤害类型\` 必须与叙事匹配：
         - 物理：刀剑拳掌
         - 内功：真气/罡气
         - 真实：穿透型绝学（稀有）
         - 混合：内外同修招式
       - \`目标类型\` 与 \`最大目标数\` 必须匹配场景规模。

    6. **附带效果规范**
       - \`附带效果[]\` 建议使用可读代码，例如：
         - \`BLEED\`（流血）、\`STUN\`（眩晕）、\`SLOW\`（迟滞）、\`SHIELD\`（护体）
       - 每项效果需明确 \`触发概率\`、\`持续时间\`、\`数值参数\`。
       - \`触发概率\` 必须 0~100，禁止“永远触发”写成 100 以外数值。

    7. **被动修正与境界特效**
       - \`被动修正\` 用于常驻增益，字段：\`属性名/数值/类型\`。
       - \`境界特效\` 是分重数解锁奖励，\`解锁重数\` 必须在合法范围内。
       - 不得在未达重数时提前生效境界特效。

    8. **武器与境界限制**
       - \`武器限制\` 为空数组代表空手或不限。
       - 场景中武器不符时：应判定“招式威力衰减或无法施展”，而不是照常满伤。
       - \`境界限制\` 不满足时，只允许尝试失败/反噬，不允许稳定释放。

    9. **输出要求**
       - 生成或修改功法时，必须返回可直接写入 \`功法结构\` 的完整 JSON。
       - 禁止漏字段、禁止伪字段、禁止把武侠术语写成现代 MMO 套话。
    `,
    类型: '数值设定',
    启用: true
};
